"""Mpynode._defaults -- shipped default source strings for nodes whose
Init / Compute / Viewport tabs ship pre-populated.

 * :mod:`mpynode._defaults.file_defaults` -- mPyFile Init/Compute/Viewport.
 * :mod:`mpynode._defaults.skin_cluster_defaults` -- mPySkinCluster Init/Compute.
 * :mod:`mpynode._defaults.starter_registry` -- maps a native type + source tier
   to its starter code; consulted by the Designer virgin-create seeder so
   registered types get runnable defaults and everything else stays header-only.
"""
