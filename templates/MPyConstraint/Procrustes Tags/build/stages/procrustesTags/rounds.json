{
  "accepted": true,
  "baseline_ms": 11.1691,
  "bench": {
    "below_floor": true,
    "fingerprint": "checked (1 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "mesh <- pSphereShape1.vtx[0]"
    ],
    "reason": "baseline 11.169 ms is below the 15 ms noise floor even at the largest bench scene (geo=400 array=20000); measured anyway -- every accept must clear 1.15x on two independent timings",
    "rung": [
      400,
      20000
    ],
    "small_rung": [
      40,
      512
    ]
  },
  "best_ms": 2.776,
  "created": 1790270451.7213848,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 11.1691,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 527.88,
      "fix_rounds": 0,
      "hypothesis": "The bench seeds one tag row (clusterTags[0]='bench') against 20000 bind matrices, so the procrustes math runs on one cluster; the time goes to converting 2 x 159,602 verts to double (plus two vstack copies) and copying 20000 MMatrix into a dense vector that the clamp then slices to one row. np.take over vstack([pts, zeros]) is reproduced exactly (negative index wraps by V+1, -1 and V land on the zero row), and nbind still comes from a full logical-index scan, so every value is bit-identical.",
      "index": 1,
      "ms": 2.776,
      "note": "confirmed twice (incumbent under the noise floor): 2.634 / 2.776 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 3.0,
      "risk": "The scan must still visit every bind element's logical index (nbind = max index + 1 decides the clamp); an O(1) last-element probe that relies on sorted physical order was measured with no gain (2.86 vs 2.79 ms) and was reverted rather than take the ordering assumption.",
      "slug": "gather_member_rows",
      "speedup": 4.023451008645534,
      "theme": "Read only the cluster-member vertices straight from the raw float buffers, and only the bind matrices below min(ncl, nbind), instead of widening both 159k-vertex meshes and all 20k bind matrices into double arrays every tick."
    },
    {
      "compiled": true,
      "duration_s": 1170.346,
      "fix_rounds": 0,
      "hypothesis": "The node's own work is a small slice of the pull: in-compute timers put inputValue(mesh) and inputValue(meshOrig) at ~1 ms each (Maya copying a 160k-vert mesh across the connection) and data.setClean(aOutMatrix) at 0.75-2 ms (20000 connected outMatrix elements), all fixed by the plug contract. What the node controls: a 20000-element MArrayDataHandle walk that only exists to find nbind for min(ncl, nbind), with ncl = 1 here, and nd:: allocation overhead around a single 3x3 SVD. Since nbind >= last physical element's logical index + 1, when that already reaches ncl the walk can be skipped and rows < ncl fetched with jumpToElement, whatever the element order. Otherwise it falls back to the original walk.",
      "index": 2,
      "ms": 2.8627,
      "note": "",
      "outcome": "not-faster",
      "parity": "pass",
      "predicted_speedup": 1.08,
      "risk": "jumpToElement cost on very large ncl, so the fast path is capped at ncl <= 16 and falls back to the original walk above that.",
      "slug": "skip_bind_walk",
      "speedup": null,
      "theme": "Read only the first ncl bind matrices by logical index instead of walking all 20000 bindMatrices elements, and run the per-cluster Procrustes fit as one scalar pass instead of ~60 nd:: whole-array temporaries."
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (4.02x)",
  "rounds": 2,
  "speedup": 4.023451008645534,
  "stop_reason": "round 2 not-faster -- nothing new to compound from",
  "type_name": "procrustesTags"
}
