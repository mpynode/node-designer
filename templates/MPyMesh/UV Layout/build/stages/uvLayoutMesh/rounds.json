{
  "accepted": true,
  "baseline_ms": 21.3355,
  "bench": {
    "fingerprint": "checked (1 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "inMesh <- pSphereShape1.vtx[0]"
    ],
    "rung": [
      300,
      10000
    ],
    "small_rung": [
      40,
      512
    ]
  },
  "best_ms": 2.3044,
  "created": 1788995625.7035818,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 21.3355,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 777.131,
      "fix_rounds": 0,
      "hypothesis": "the node is O(N) marshalling, not a query: 90k out-of-line getUV(k, &setName) calls and a full 90k-vertex/90k-face MFnMesh::create dominate, while a moved vertex changes neither the UVs nor the UV topology, so the create is skippable every tick once the topology compares equal; UVs are float32 already, so an MFloatPointArray / raw float store write is bit-identical to the double->float narrowing create performs",
      "index": 1,
      "ms": 2.642,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 5.0,
      "risk": "in-place reuse depends on hOut.asMesh() still being the object this node created; guarded by MObject validity, numVertices/numPolygons, and a full byte compare of counts+indices against the ones the stored mesh was built from, with any doubt falling back to a full create. The fail-soft set selection (skip empty / unreadable sets, first set unless uvSetName matches) is preserved verbatim.",
      "slug": "bulk_uvs_inplace_output",
      "speedup": 8.07551097653293,
      "theme": "read the UV set with the two bulk MFnMesh calls instead of ~90k per-UV getUV calls, and when the output handle still holds a mesh built from byte-identical UV counts/indices rewrite only its (u, v, 0) positions in place instead of an MFnMesh::create of ~90k vertices"
    },
    {
      "compiled": true,
      "duration_s": 812.965,
      "fix_rounds": 0,
      "hypothesis": "a stage timer showed the node's own work was almost all marshalling: getUVs + two MFloatArray::get copies (~300-600 us), two MIntArray::get copies (~300 us) and filter/guard/compare loops (~150 us) around two Maya calls (input pull ~750 us, getAssignedUVs ~700 us) that cannot be avoided soundly; getRawUVs is a pointer into the mesh's (u, v) store (verified byte-equal to getUVs on 90,599 UVs), and MIntArray::operator[] returns a real int& so &arr[0] (contiguity confirmed against &arr[n-1], get() fallback otherwise) is a zero-copy view; keying the derived topology on a memcmp of the RAW counts/ids lets a hit skip the filter and guard loops entirely",
      "index": 2,
      "ms": 2.3044,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.4,
      "risk": "getRawUVs layout (interleaved vs planar) -- checked in-run against getUVs, 0 mismatches; MIntArray storage contiguity -- checked per call, falls back to get(); a second experiment, pre-sizing the MIntArrays with setLength instead of clear() so getAssignedUVs could reuse the buffer, measured 1.790 vs 1.711 ms and was reverted; remaining time (~1.45 of ~1.65 ms per tick) is the input mesh pull and getAssignedUVs inside Maya",
      "slug": "raw_uv_and_int_views",
      "speedup": 9.258592258288493,
      "theme": "read the UV set through MFnMesh::getRawUVs and view the getAssignedUVs MIntArrays in place, so the per-tick cost is one memcmp cache key plus the two Maya calls instead of four bulk copies"
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (9.26x)",
  "rounds": 2,
  "speedup": 9.258592258288493,
  "stop_reason": "round 2 gained 1.15x, below the 1.15x needed to continue",
  "type_name": "uvLayoutMesh"
}
