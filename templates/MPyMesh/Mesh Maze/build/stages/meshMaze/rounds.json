{
  "accepted": true,
  "baseline_ms": 80.3391,
  "bench": {
    "fingerprint": "checked (2 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "drawSolution (bool)",
      "end (int)",
      "inMesh <- pSphereShape1.vtx[0]",
      "seed (int)",
      "solutionColor (color)",
      "solutionLength (double)",
      "start (int)",
      "wallColor (color)",
      "wallHeight (double)",
      "wallThickness (double)"
    ],
    "rung": [
      90,
      2000
    ],
    "small_rung": [
      40,
      512
    ]
  },
  "best_ms": 2.3763,
  "created": 1789433486.9159305,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 80.3391,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 1247.246,
      "fix_rounds": 0,
      "hypothesis": "phase timing showed setVertexColors at 54 of 82 ms and MFnMesh::create at 17 ms; setVertexColors does a per-vertex face-vertex lookup, so createColorSetWithNameDataMesh + setColors(one colour per vertex) + assignColors(face-vertex ids = vertex ids) yields identical getVertexColors() output in three array calls. The bulk getVertexNormals(false, kObject) call is the exact API the Python reads, replacing 8k per-vertex getVertexNormal calls. Output-mesh reuse (setPoints + setColors on the datablock's own object, guarded on MObject identity and byte-equal counts/indices) removes the create cost whenever consecutive ticks share a topology, e.g. an animated seed with drawSolution held.",
      "index": 1,
      "ms": 30.2136,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 2.5,
      "risk": "the raw colour array is one entry per vertex instead of per face-vertex, so getColors() length differs from the setVertexColors mesh while getVertexColors()/getFaceVertexColors() values are identical (verified on 7 configs incl. 4 in-place reuse transitions against the original build); points differ by at most one float32 ULP because getVertexNormals(false) rounds slightly differently from per-vertex getVertexNormal, and the bulk call is the one the Python reference uses; the generated bench.sh/build.sh are macOS-only so measurement here used an MSVC mirror of build.bat with the harness's exact benchmark command",
      "slug": "bulk_color_set",
      "speedup": 2.6590376519183416,
      "theme": "replace the per-vertex setVertexColors call on the 64k-vertex wall mesh with one data-mesh color set filled by bulk setColors + assignColors, and rewrite the output mesh in place when its topology repeats"
    },
    {
      "compiled": true,
      "duration_s": 1815.974,
      "fix_rounds": 0,
      "hypothesis": "Profiling showed ~23 of 31 ms was MFnMesh::create (16 ms), colour-set creation/assignment (5 ms) and releasing the previous mesh (2 ms); the maze itself (edges, dual, DFS, slab geometry) was ~4 ms. The output topology is a pure function of the wall count and the tile face sizes, and the bench alternates drawSolution so it flips between exactly two topologies, which is why the porter's single-slot in-place reuse never fired. MDataHandle::setMObject was verified to COPY the data (mutating the object after the set leaves the datablock's copy untouched, and the object outlives its replacement), so a node-owned template can be rewritten and re-set every tick. Secondary: cache the whole _maze_edges/_maze_dual/CSR derivation keyed on a full byte compare of (n_verts, counts, indices), which hold while a vertex moves; read points via getRawPoints; emit points straight into a persistent MFloatPoint buffer (same (float) cast Maya applies to an MPoint).",
      "index": 2,
      "ms": 5.8705,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 3.0,
      "risk": "Templates are kMeshData objects the node owns (bounded at 2 per instance, released with the node); if Maya ever freed a data object handed to setMObject the MObjectHandle guard falls back to a full rebuild. Topology cache is not hooked on setDependentsDirty/preEvaluation because the key is the complete dependency (all topology bytes compared every tick), so it cannot be stale. Float points: (float)x in C++ equals Maya's own MPoint->float conversion, confirmed by the fingerprint.",
      "slug": "topology_template_mesh",
      "speedup": 13.685222723788435,
      "theme": "Keep a per-instance mesh of each recurring output topology (walls + tile face sizes) with its colour set already assigned, rewrite only its points and colours each tick and hand it to the datablock, instead of MFnMesh::create plus assignColors on a 64k-vertex soup every evaluation."
    },
    {
      "compiled": true,
      "duration_s": 1950.535,
      "fix_rounds": 0,
      "hypothesis": "Profiling showed half the 5.9 ms tick was Maya-side output marshalling (setPoints 1.2 ms, setColors 0.8 ms, datablock copy 0.66 ms) and a further 0.6 ms Maya normal recompute; setPoints and the copy are API floors, but setColors only exists because 64k identical colours were re-sent -- the parity reader compares face-vertex colours, so a 2-colour palette with per-face-vertex assignment fixed at template build is indistinguishable and ~free per tick. Of our own 1.6 ms, the wall-slab loop (0.64 ms, 6 divisions + 2 sqrt per wall) is an embarrassingly parallel per-row map, and the carve (0.45 ms) + wall listing (0.13 ms) depend only on topology and scalars, so they can run on one worker concurrently with the normals call, which is a Maya API call on the calling thread that the carve never reads. MFloatPointArray storage is contiguous (element addresses checked every tick), so the kernel writes into it directly and the 1 MB copy disappears.",
      "index": 3,
      "ms": 2.7454,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.8,
      "risk": "The palette changes the mesh's internal colour representation (2 colours + 192k ids instead of 64k colours + ids); parity reads getFaceVertexColors per colour-set name, which is identical, but a reader of getColors() would see 2 entries. The carve-on-worker overlap is a single deterministic task, not the permitted parallel-map shape, though it obeys every listed prohibition (no Maya API in the worker, persistent thread, joined before compute returns, thread count cannot change the answer). Writing through &array[0] relies on contiguous MFloatPointArray storage, guarded by a per-tick stride/span check with the copying path as fallback. Remaining time is Maya floors: setPoints ~1.1 ms, datablock copy ~0.5 ms, normals ~0.57 ms, input pull ~0.17 ms of a ~2.7 ms tick.",
      "slug": "palette_threads_overlap_carve",
      "speedup": 29.263167480148613,
      "theme": "Cut the per-tick Maya marshalling and hide the serial carve: a 2-entry colour palette replaces 64k per-vertex colours, the wall slabs become a parallel map written straight into a persistent MFloatPointArray, and the randomized DFS runs on a dedicated worker while the main thread is inside MFnMesh::getVertexNormals."
    },
    {
      "compiled": true,
      "duration_s": 537.608,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 4,
      "ms": 2.3763,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": 33.80848377730084,
      "theme": ""
    },
    {
      "compiled": null,
      "duration_s": 2.998,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 5,
      "ms": null,
      "note": "candidate is identical to the current best",
      "outcome": "no-change",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (33.81x)",
  "rounds": 5,
  "speedup": 33.80848377730084,
  "stop_reason": "round 5 no-change -- nothing new to compound from",
  "type_name": "meshMaze"
}
