{
  "accepted": true,
  "baseline_ms": 46.463,
  "bench": {
    "fingerprint": "checked (1 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "defaultColor (color)",
      "inMesh <- pSphereShape1.vtx[0]"
    ],
    "rung": [
      40,
      512
    ]
  },
  "best_ms": 11.0488,
  "created": 1789399138.0479763,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 46.463,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 1317.584,
      "fix_rounds": 0,
      "hypothesis": "a section profile at the bench size (T=3120 tris, M=32.6k candidate pairs, P=19.2k overlaps, m=4780 cubes -> 38k verts, 115k face-vertices) showed all geometry math ~5 ms while setVertexColors took ~30 ms and MFnMesh::create ~8.5 ms. setVertexColors stores ONE colour per face-vertex (numColors = 24m, RGBA, unclamped, colorSet1 current), so createColorSetWithNameDataMesh + setColors(face-vertex table) + assignColors(0..n-1) reproduces it in two bulk calls. Between ticks only defaultColor and vtx[0] move, the voxel count (hence counts/indices) does not change, so the previous output data object (checked by MObject identity, byte-compared counts/indices, and O(1) vertex/face/face-vertex/colour counts) can be updated with setPoints + setColors. The winner pick is a per-cell argmin with lowest-index tie-break; a strict-< pass in pair order over a dense key array gives exactly the stable sort's first-per-key, in ascending key order",
      "index": 1,
      "ms": 11.0488,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.8,
      "risk": "colour-set layout and the in-place path must match the fresh-create output exactly: verified IDENTICAL against the original build on 14 scenes (default colour moving, vertex-colour input, voxel-size changes, a second node on a cube, re-presented earlier input, and four whole-mesh shifts that move every point at a constant voxel count), comparing points, counts, indices, colour-set names, current set, face-vertex/vertex colours, the colour table, representation and clamping; the harness point fingerprint is also identical. Fallbacks: setVertexColors if the direct colour write fails, a fresh create if any reuse guard fails, the original merge sort when the key range is large or any dist2 is NaN. NOTE: build.sh is macOS-only and cannot build on this Windows machine, so every number here comes from an MSVC mirror of bench.sh (the flags of mpynode's own Windows build.bat, then the identical benchmark_node.py call); local baseline was 48.07 ms against the 46.46 ms quoted",
      "slug": "direct_colorset_write",
      "speedup": 4.205253059155745,
      "theme": "the node was bound by Maya mesh-output calls, not voxel math: write the colour set directly in setVertexColors' exact layout instead of calling setVertexColors (48.1 -> 24.9 ms), rewrite points + colours IN PLACE into the node's own output mesh when its topology is byte-identical instead of MFnMesh::create each tick (-> 12.1 ms), and replace the O(P log P) winner sort with a dense one-pass best-per-cell pick plus reused scratch buffers (-> 10.0 ms)"
    },
    {
      "compiled": null,
      "duration_s": 1230.877,
      "fix_rounds": 0,
      "hypothesis": "a per-phase profile of the 10 ms tick showed ~2.1 ms building/copying MPoint, MColor and counts/indices vectors, ~4.7 ms in the in-place write-back and ~2.5 ms in the SAT loop; the unwelded-cube topology is a pure function of the cube count, and a triangle's surviving (cell, dist2, barycentric) rows depend only on its three corners + voxelSize + SAT half-size, so a moved vertex should cost its ~6 triangles, not all 3120",
      "index": 2,
      "ms": null,
      "note": "candidate is identical to the current best",
      "outcome": "no-change",
      "parity": null,
      "predicted_speedup": 2.2,
      "risk": "corner points are rounded double->float by a plain cast, which must equal Maya's MPoint->float storage rounding; the colour-set layout (one colour per face-vertex, id j for face-vertex j) is kept exactly; the SAT cache is keyed per row on the exact bytes of the three corners, voxelSize, the SAT half-size (tol) and the triangle list, with packed keys re-derived whenever base/span move, and is also invalidated by setDependentsDirty/preEvaluation when voxelSize dirties, so survivor order (the pick's tie-break order) is unchanged; the harness fingerprint compares points only, so colours were kept identical by construction; clang/libc++ was not available here to compile-check, and the row-splice path (a moved triangle changing its survivor count) is not exercised by this bench's 1e-3 moves",
      "slug": "float4_output_and_row_sat_cache",
      "speedup": null,
      "theme": "write cube corners and face-vertex colours straight into float4 buffers keyed on the cube count (no MPoint/MColor/topology vectors, no topology byte-compare), cache each triangle's SAT survivors across evaluations so only the triangles touching the moved vertex are re-tested (overwritten in place when their survivor count holds), and reuse one MColorArray for the in-place colour upload"
    }
  ],
  "max_rounds": 8,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (4.21x)",
  "rounds": 2,
  "speedup": 4.205253059155745,
  "stop_reason": "round 2 no-change -- nothing new to compound from",
  "type_name": "voxelizeMesh"
}
