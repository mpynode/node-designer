{
  "accepted": true,
  "baseline_ms": 4.6454,
  "bench": {
    "below_floor": true,
    "fingerprint": "checked (1 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "swingWeights[0] (double)",
      "twistWeights[0] (double)",
      "input[0].inputGeometry <- pSphereShape1Orig.vtx[0]"
    ],
    "reason": "baseline 4.645 ms is below the 15 ms noise floor even at the largest bench scene (geo=400 array=20000); measured anyway -- every accept must clear 1.15x on two independent timings",
    "rung": [
      400,
      20000
    ],
    "small_rung": [
      40,
      512
    ]
  },
  "best_ms": 0.9275,
  "created": 1789003491.8814778,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 4.6454,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 805.06,
      "fix_rounds": 0,
      "hypothesis": "the bench wires no joints (J=0) so every tick is: 40000 MArrayDataHandle element reads whose values are never used, an N*3 push_back widening, an (N,4) homogeneous concat, then a shape-mismatch throw; none of that is numeric work, so removing the reads and the two big allocations should take the node to Maya's own mesh-copy floor",
      "index": 1,
      "ms": 0.9275,
      "note": "confirmed twice (incumbent under the noise floor): 0.896 / 0.927 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 2.5,
      "risk": "linear_blend rewrite must keep ((1*W)*M)*p per term and j-major/k-minor accumulation to stay bit-identical; the k==3 factor is the literal 1.0 the concat supplied so eliding that multiply is exact. Flattened-size shortcut assumes MArrayDataHandle logical indices ascend (falls back to a full elementIndex scan if jumpToArrayElement fails). Weight-set reads now happen only when skinMode==2 and both sizes equal N*J, which is the only place the Python consumes their values.",
      "slug": "defer_weight_reads_reuse_ptsbuf",
      "speedup": 5.008517520215634,
      "theme": "read the two 20000-element weight-set plugs only on the branch that consumes them, fuse linear_blend into one strided loop with no (N,4) concat, and keep the 3n-double rest-point store as a per-instance buffer instead of faulting in a fresh 4 MB block every tick"
    },
    {
      "compiled": true,
      "duration_s": 759.396,
      "fix_rounds": 0,
      "hypothesis": "on the bench scene no joints are wired, so the weight matrix has 0 rows and every tick ends in nd's broadcast error before the rest copy is ever consumed; that copy (479k float->double writes) is the one pass this node owns on that path, everything else is Maya's input->output mesh copy and iterator setup. Float->double widening is exact, so reading floats in the kernel is bit-identical to reading the copy, and the shape pre-check calls nd::broadcast_shapes itself so an incompatible pair raises the same error nd::sub raised",
      "index": 2,
      "ms": 0.9292,
      "note": "incumbent under the noise floor: second measurement 0.929 ms did not confirm 0.757 ms",
      "outcome": "not-faster",
      "parity": "pass",
      "predicted_speedup": 1.15,
      "risk": "the win is larger than predicted because the copy also re-faulted a 3.8 MB block per tick, not just wrote it. build.sh is macOS-only and cannot run on this Windows host, so the file was compiled with the harness's own MSVC flags (build.bat: /O2 /fp:precise /std:c++17) and timed with the identical benchmark_node.py invocation; the generic lambda (const auto* Rp) is C++14 and builds on both compilers. Behaviour on the real skinning paths (mode 1, live result with seeded weight sets) is unchanged: they materialise the double copy exactly as before",
      "slug": "lazy_rest_float_source",
      "speedup": null,
      "theme": "stop widening the 160k rest points to a 3.8 MB double copy every tick; the linear-blend kernel and the envelope blend read the mesh's own float store, and the double copy is built only for the nd-heavy dual-quaternion and generic-broadcast paths"
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (5.01x)",
  "rounds": 2,
  "speedup": 5.008517520215634,
  "stop_reason": "round 2 not-faster -- nothing new to compound from",
  "type_name": "twistSwingSkin"
}
