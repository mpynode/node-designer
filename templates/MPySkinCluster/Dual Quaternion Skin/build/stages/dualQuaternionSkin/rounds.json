{
  "accepted": true,
  "baseline_ms": 2.1863,
  "bench": {
    "below_floor": true,
    "fingerprint": "checked (1 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "input[0].inputGeometry <- pSphereShape1Orig.vtx[0]"
    ],
    "reason": "baseline 2.186 ms is below the 15 ms noise floor even at the largest bench scene (geo=400 array=20000); measured anyway -- every accept must clear 1.15x on two independent timings",
    "rung": [
      400,
      20000
    ],
    "small_rung": [
      40,
      512
    ]
  },
  "best_ms": 0.7029,
  "created": 1789003448.5450327,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 2.1863,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 816.124,
      "fix_rounds": 0,
      "hypothesis": "the bench attaches the skinCluster with a bare cmds.deformer (J=0 joints, no weights), so every tick spent ~0.5 ms widening 159602x3 floats into a std::vector<double> via push_back and allocating ~80 zero-length nd::Array temporaries before the Rn reshape threw; harvesting influences first and building nothing until the shapes are known removes all of that, and at J>0 the fused kernel keeps the exact per-element expressions and left-to-right accumulation of the reference (matmul l-order, sum_mul from 0, einsum j-order, the 4-way quaternion branch as a select) so the answer is bit-identical; sparse weight rows are walked only when every joint's dual quaternion is finite, since 0*finite is an exact zero that leaves a running sum unchanged, with a dense fallback otherwise",
      "index": 1,
      "ms": 0.8581,
      "note": "confirmed twice (incumbent under the noise floor): 0.858 / 0.776 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.3,
      "risk": "the bench cannot see the J>0 kernel (it throws at J=0 exactly as the reference did, message and status preserved), so its correctness rests on mirroring operation order rather than on the timing; the remaining ~0.57 ms is Maya's own input->output mesh copy plus the pull, which the node cannot touch. Threading was deliberately not added: the per-vertex pass never runs under this bench, so a pool would be unmeasured risk. Measured on the Windows twin of bench.sh (bench.sh itself is the macOS variant and cannot run here): 1.850 ms -> 0.574 ms median of 9. Checked with verify._verify_one on the 3-joint arm rig, 10 random poses, compiled vs interpreted: pass, maxerr 0.0.",
      "slug": "fused_raw_dqs_kernel",
      "speedup": 2.547838247290526,
      "theme": "replace the ~80 whole-array nd:: temporaries of the lowered deform with one per-joint pass and one fused per-vertex pass that reads and writes the mesh's raw floats directly"
    },
    {
      "compiled": true,
      "duration_s": 1104.478,
      "fix_rounds": 0,
      "hypothesis": "profiling deform() at 160k verts put MFnMesh construction + numVertices() at 32-80us of the ~100us the node itself spends per tick (iter.count() is <1us, getRawPoints <1us, the three empty array harvests ~3us, displayError ~20us, the C++ throw ~10us); the benchmark seeds no matrix/bindPreMatrix/weightList elements so every tick fails the skinN != N check, and the original harvested the mesh first -- moving the checks ahead of the attach and reporting them without a throw removes ~60us of a ~650us tick, with the success path unchanged (N is iter.count() on both paths, allPositions() returns exactly count() points)",
      "index": 2,
      "ms": 0.7029,
      "note": "confirmed twice (incumbent under the noise floor): 0.703 / 0.603 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.1,
      "risk": "the timed tick is ~85% Maya's own input->output mesh copy and evaluation, so the ceiling is small and run-to-run noise between mayapy processes (0.59-0.81 ms for the same binary) exceeds the gain -- decided on two interleaved A/B rounds at 31 iterations (5 of 6 pairs won, medians 0.52-0.55 vs 0.59-0.67 ms) and a bitwise point comparison against the baseline build on 3-joint full- and partial-membership rigs over 7 poses; the weights child attribute is now resolved once into a function-local static, which is a class-static attribute object, not per-node state",
      "slug": "defer_mesh_attach",
      "speedup": 3.1103997723716037,
      "theme": "read the membership count from the iterator and run every input-shape check before attaching an MFnMesh to the freshly copied output mesh, so a tick that cannot skin never pays for the attach"
    },
    {
      "compiled": null,
      "duration_s": 827.459,
      "fix_rounds": 0,
      "hypothesis": "instrumented deform(): harvest 3 us + displayError 17 us, then the skinN != N error return -- so the 0.7 ms is the input->output copy, the MItGeometry build and the upstream pull. Overriding compute() with MDataHandle::copy (measured 2 us, it is a shared reference) plus a read-only getGeometryIterator (6-17 us vs 850 us writable) should have removed the copy and the iterator cost",
      "index": 3,
      "ms": null,
      "note": "candidate is identical to the current best",
      "outcome": "no-change",
      "parity": null,
      "predicted_speedup": 2.0,
      "risk": "a plug-in-side pull of input[idx].inputGeometry (inputArrayValue+inputValue: 0.6+0.8 ms; child MPlug inputValue: 2.4-3.5 ms) costs more than Maya's whole default compute, so the override ticked at 1.95 ms vs 0.76 ms baseline. Every variant was reverted; the shipped file is byte-identical to the one handed in and rebuilt/re-benched last (0.684 ms). A real skin path (J>0, weights for every vertex) is not exercised by this scene, so no unmeasured edit was made there either",
      "slug": "override_compute_reverted",
      "speedup": null,
      "theme": "on this bench scene (no joints, no weights) deform() is ~20 us of a ~700 us tick; the rest is Maya's MPxGeometryFilter plumbing, and overriding compute() to take control of it measured 1.4-2.5x SLOWER, so the file is left at the baseline"
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (3.11x)",
  "rounds": 3,
  "speedup": 3.1103997723716037,
  "stop_reason": "round 3 no-change -- nothing new to compound from",
  "type_name": "dualQuaternionSkin"
}
