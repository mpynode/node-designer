{
  "accepted": true,
  "baseline_ms": 0.0127,
  "bench": {
    "below_floor": true,
    "fingerprint": "checked (2 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "matrix0 (matrix)",
      "matrix1 (matrix)",
      "parentWorld (matrix)"
    ],
    "reason": "baseline 0.013 ms is below the 15 ms noise floor even at the largest bench scene (geo=400 array=20000); measured anyway -- every accept must clear 1.15x on two independent timings",
    "rung": [
      400,
      20000
    ],
    "small_rung": [
      40,
      512
    ]
  },
  "best_ms": 0.004,
  "created": 1789004691.6762154,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 0.0127,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 1081.839,
      "fix_rounds": 0,
      "hypothesis": "This node is ELEMENTWISE with a fixed 4x4 output; the arithmetic is ~200 ns, so the 12 us tick is all per-access overhead: MFnDependencyNode + 3x findPlug by name + 3x MPlug::getValue DG pulls in desiredLocal(), then ~30 shared_ptr/vector allocations for slice/norm/cross/eye/assign/inv/matmul temporaries. Keeping every operation in the same order on registers (sub, acc=acc+x*x norm, dot1d acc+=, cross formula, Gauss-Jordan inv with the 1e-12 pivot guard, matmul2d_fixed acc+=) is bit-identical under -ffp-contract=off / /fp:precise, so the kernel can be a plain function with zero allocations.",
      "index": 1,
      "ms": 0.0073,
      "note": "confirmed twice (incumbent under the noise floor): 0.004 / 0.007 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 3.0,
      "risk": "The datablock handle value must equal what MPlug::getValue returned (same plug, same evaluation) -- and it does, the original already pulled the same handles for the freeze fix. The Gauss-Jordan port must keep nd::inv's exact pivot order, division-by-copied-d and f==0 skip, or a 1-ULP drift appears in inv(P). The in-place write relies on the 16 elements existing in logical order; any deviation falls back to the original builder path.",
      "slug": "fused_scalar_kernel",
      "speedup": 1.7397260273972601,
      "theme": "Replace the nd:: array chain (30-odd heap allocations per evaluate) and three findPlug/getValue/MFnMatrixData round trips with one fused scalar kernel that reads the three matrices straight from the datablock handles compute() already pulls; then write the 16 output elements in place instead of through a rebuilt MArrayDataBuilder."
    },
    {
      "compiled": true,
      "duration_s": 609.251,
      "fix_rounds": 0,
      "hypothesis": "compute() is ~85 Maya API calls and no real arithmetic (three 4x4 reads, one 4x4 inverse, sixteen scalar writes); the 16-element write loop is 64 of those calls, so trimming 14 redundant elementIndex() checks (16 sorted distinct indices with first==0 and last==15 are exactly 0..15) should be the largest removable share. Every input moves every tick, so no derived state survives between evaluations and caching cannot apply.",
      "index": 2,
      "ms": 0.004,
      "note": "confirmed twice (incumbent under the noise floor): 0.004 / 0.004 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.1,
      "risk": "the order guard assumes MArrayDataHandle stores elements sorted by logical index; this array is non-writable, non-storable and only ever built 0..15 by this compute, and the harness fingerprint (all 16 elements + matrix) was byte-identical to the baseline. Measured: neutral -- 0.0037 ms before and after when the process lands on a fast core. Run-to-run the harness is bimodal on this hybrid CPU (0.0037 vs 0.0069-0.0072 ms with tight samples inside each run, even pinned to CPUs 0-7); a diagnostic build that skipped asMatrix/inverse/aim math entirely also measured 0.0069 ms, so the node's own work is below the noise of the pull itself and further compute-side edits cannot be judged at this scene.",
      "slug": "lean_output_walk",
      "speedup": 3.175,
      "theme": "walk the 16 output elements with next() and check the logical index only at the two ends, so each element costs 3 Maya API calls instead of 4"
    },
    {
      "compiled": true,
      "duration_s": 793.539,
      "fix_rounds": 0,
      "hypothesis": "the node is at the harness floor: a no-op compute measured 2.8-3.0 us against a 3.2-4.0 us baseline, so the whole controllable cost is under 1 us (about 0.25 us math, about 0.5 us for the 16 MArrayDataHandle element writes, the rest preamble). Nothing here is query-shaped or cacheable -- every input moves every tick -- so the only bit-identical levers are removing dead work: 3 sqrt + 9 divides on rows nd_gate_mix never reads when every gate is open, an RTTI dynamic_cast + thisMObject() paid on EVERY plug routed through the override (including `matrix`, pulled each tick), and a double[4][4] -> MMatrix copy-construct",
      "index": 3,
      "ms": 0.0035,
      "note": "incumbent under the noise floor: 1.15x required, measured 1.14x",
      "outcome": "not-faster",
      "parity": "pass",
      "predicted_speedup": 1.04,
      "risk": "gain is inside the +-15% run-to-run noise of a 3 us measurement and cannot clear the 1.15x below-floor gate; measured 3.2-3.3 us vs 3.2-4.0 us baseline on quiet runs (some runs on this machine bimodally jump to ~7 us from background load). Correctness verified bit-identical on all 16 outputs across 9 input sets including the coincident-points and up-vector-switch branches, worldMatrix within 9e-16 of the numpy reference. The workspace bench.sh/build.sh are the macOS recipe and cannot run on this Windows host; measurements were taken through an untouched-harness mirror (same benchmark_node.py invocation and arguments, MSVC /O2 /fp:precise compile as the driver's own gate uses)",
      "slug": "trim_preamble_gate_all",
      "speedup": null,
      "theme": "test the plug before the RTTI cast so the base-class `matrix` pull skips it, and specialise the all-gates-open mix so the three unused base-row normalisations are never computed"
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (3.17x)",
  "rounds": 3,
  "speedup": 3.175,
  "stop_reason": "round 3 not-faster -- nothing new to compound from",
  "type_name": "aimTransform"
}
