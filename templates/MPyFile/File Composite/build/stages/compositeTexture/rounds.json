{
  "accepted": true,
  "baseline_ms": 24.1295,
  "bench": {
    "fingerprint": "checked (2 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "borderColor (color)",
      "opacities[0] (float)",
      "preFilter (bool)",
      "preFilterRadius (float)",
      "uvCoord (float2)"
    ],
    "rung": [
      "bake",
      1024
    ]
  },
  "best_ms": 2.0255,
  "created": 1789007977.676351,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 24.1295,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 579.27,
      "fix_rounds": 0,
      "hypothesis": "With one bogus layer over 512 opacities the bake is the 256x256 fallback grid, so per-texel setup (several mallocs + a 4 KB opacity copy) dwarfs the actual sample; hoisting it removes ~all node-side work. Follow-ups measured in the same run: size-gate the row threading so a sub-millisecond bake runs serially instead of spawning 11 threads (16.83 -> 7.28 -> 3.35 ms), then drop unloadable layers with finite opacity from the table since their alpha is exactly +-0 and the accumulate is an identity (3.35 -> 3.05 ms).",
      "index": 1,
      "ms": 3.0149,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 2.0,
      "risk": "The missing-layer skip can flip the sign of a zero in an accumulator (0*op with op<0 is -0.0), never a non-zero value; non-finite opacities are kept so 0*inf still poisons the stack. bench.sh is a macOS script (clang -bundle, /Applications/Autodesk) and cannot run on this Windows box; every number here came from an untouched twin that compiles the same file with MSVC (/O2 /fp:precise) to an .mll and runs the identical benchmark_node.py invocation (bake, 1024 source, 512-element arrays, 9 iters). Local MSVC baseline was 16.83 ms, not the 24.13 ms quoted on entry.",
      "slug": "resolve_layers_once",
      "speedup": 8.003416365385254,
      "theme": "The VP2 bake resolved the layer stack per texel -- a heap vector of layer paths, a heap copy of the 512 opacities as an nd::Array, a 2-element uv array and a string-keyed memo probe per layer -- so resolve it once per bake into a flat {buffer, size, opacity} table and run a lean per-texel kernel over it."
    },
    {
      "compiled": true,
      "duration_s": 571.241,
      "fix_rounds": 0,
      "hypothesis": "instrumented breakdown per tick: updateDG 150 us (512 elementByPhysicalIndex), texName 105 us (512 snprintf), bake 550 us (65k texels x 0 layers = writing zeros, plus 1 MB fresh-allocation page faults), acquireTexture 110 us; the first three are removable exactly because an empty stack is bit-identical +0.0f and a surplus opacity never reaches the result",
      "index": 2,
      "ms": 2.0255,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.45,
      "risk": "texture key now covers only opacities that can affect the bake, which is exactly the set composite_layers reads; the remaining ~1.8 ms is ogsRender + acquireTexture upload and not reachable from this file",
      "slug": "skip_empty_bake_reuse_buffer",
      "speedup": 11.912861021969883,
      "theme": "the bench stack has zero loadable layers (layers[0]='bench'), so the whole 256x256 bake was a zero-fill: skip the texel loop when the resolved stack is empty, keep the bake grid in a reused member buffer instead of page-faulting a fresh 1 MB vector each tick, and read/key only the opacities[i] with i < layers.size() (the only ones composite_layers can see) instead of 512 MPlug reads plus 512 snprintf per tick"
    },
    {
      "compiled": true,
      "duration_s": 695.807,
      "fix_rounds": 0,
      "hypothesis": "an in-node steady_clock profile showed the override at ~130-200 us of a ~2100 us tick (updateDG 22, key+findTexture 28, bake path 22, acquireTexture 80, nrl=0, 256x256 fallback grid because the bench's layers[0]='bench' never loads); the old key hashed borderColor, all opacities and the unresolved paths, but the bake samples with wrap fixed to clamp so borderColor is never read, and a slot that does not load with a finite opacity is an exact no-op -- so every bench tick re-baked and re-uploaded identical bytes. Keying on (path, colorSpace, preFilter, kernel, rq, w, h, op) per RESOLVED layer plus the grid size makes those ticks a texture-manager hit, removing bake+acquire (~100 us) and leaving ~45 us of override work. Second, smaller change in the same round: updateDG built ten plugs by name (findPlug walks the attribute table per call); MPlug(node, attr) from the class's static attribute MObjects is the same non-networked plug without the lookup. Bench numbers were taken on this Windows host by compiling with the engine's own MSVC recipe (porter.compile_cpp, /O2 /fp:precise) and running the identical benchmark_node.py invocation against the .mll, because build.sh/bench.sh are clang -bundle/lipo only. Baseline via that path: 2.13-2.37 ms (9 iters); exact key alone: 1.828 ms (21 iters); exact key + direct plugs (final): 1.701 ms (21 iters, samples 1.59-2.43).",
      "index": 3,
      "ms": 2.0812,
      "note": "",
      "outcome": "not-faster",
      "parity": "pass",
      "predicted_speedup": 1.06,
      "risk": "the compute() path and every texel value are untouched, so parity cannot move; the key is content-addressed (path + the pixel cache's own settings + op as %.17g), never a pointer, so two nodes or a re-decoded image cannot alias. A non-finite opacity on a missing slot is kept and keyed by value, exactly as nd_resolve_layers keeps it. The remaining ~1.9 ms is Maya's ogsRender + shader/texture plumbing outside this file, so further in-file rounds are under the +-0.3 ms sample noise.",
      "slug": "exact_bake_key",
      "speedup": null,
      "theme": "key the VP2 texture cache on what the bake actually reads (the resolved layer stack and grid size) instead of every raw input, so an animated borderColor or an opacity on an empty slot no longer forces a re-bake and GPU upload of byte-identical pixels"
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (11.91x)",
  "rounds": 3,
  "speedup": 11.912861021969883,
  "stop_reason": "round 3 not-faster -- nothing new to compound from",
  "type_name": "compositeTexture"
}
