{
  "accepted": true,
  "baseline_ms": 0.3461,
  "bench": {
    "below_floor": true,
    "fingerprint": "checked (2 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "borderColor (color)",
      "opacities[0] (float)",
      "preFilterRadius (float)",
      "uvCoord (float2)"
    ],
    "reason": "baseline 0.346 ms is below the 15 ms noise floor even at the largest bench scene (geo=400 array=20000); measured anyway -- every accept must clear 1.15x on two independent timings",
    "rung": [
      400,
      20000
    ],
    "small_rung": [
      40,
      512
    ]
  },
  "best_ms": 0.0025,
  "created": 1788993220.3920677,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 0.3461,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 566.022,
      "fix_rounds": 0,
      "hypothesis": "the composite consumes ops[i] only for i < layers.size() (missing element -> 1.0, surplus ignored), so under the bench's one-layer stack the 20000-element MArrayDataHandle walk (elementIndex/inputValue/asFloat/next per element) is the entire cost; jumpToElement(i) for the handful of needed indices removes it and leaves compute() at Maya's dgdirty+pull floor",
      "index": 1,
      "ms": 0.0045,
      "note": "confirmed twice (incumbent under the noise floor): 0.004 / 0.004 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 10.0,
      "risk": "none numerically -- the dense-by-logical-index vector the port built (holes = 1.0) and the composite's own i<nOps?ops[i]:1.0 rule mean a missing element already read as 1.0, which is exactly what a failed jumpToElement writes; a deep stack (layers.size()*4 >= elementCount) keeps the linear walk so the jumps never lose there",
      "slug": "lazy_opacity_read",
      "speedup": 76.91111111111113,
      "theme": "read only the opacity elements the layer stack can use (logical index < layers.size()) instead of walking all 20000 through the Maya array handle"
    },
    {
      "compiled": true,
      "duration_s": 971.435,
      "fix_rounds": 0,
      "hypothesis": "the bench stack is one layer with no image and the memo hits every tick, so compute() is pure marshalling: a dozen heap round-trips (vector<string>, three shared_ptr-backed nd::Array, two slice views, an MString rebuilt from std::string) were ~1.4 us of a ~2.3 us compute(); an allocation-free loop with the identical float->double->float conversion order removes that level of work while every value stays byte-identical. Measured with an in-node steady_clock profile: texel 1400 ns -> 250 ns, compute() 2300 ns -> 1200 ns; harness 0.0036 -> 0.0027 ms. Note: the workspace build.sh/bench.sh are macOS-only (clang -bundle, lipo) and cannot run on this Windows host, so the numbers were taken by compiling the same file with the engine's own MSVC recipe (porter.compile_cpp, /O2 /fp:precise) and running the identical benchmark_node.py invocation against the .mll.",
      "index": 2,
      "ms": 0.0031,
      "note": "confirmed twice (incumbent under the noise floor): 0.003 / 0.003 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.4,
      "risk": "the remaining 1.2 us is Maya API handle reads/writes and the harness's own Python->DG dispatch (~1.5 us), so further rounds are at the noise floor (samples vary +-0.0002 ms); the VP2 bake shares nd_texel and gets the same allocation-free kernel, which was not separately re-timed here. Parity self-check: the original .mll and this build were driven through 423 input combinations with three real PNG layers plus a blank and a missing slot (3 colour spaces x 5 prefilter settings x 4 wrap-mode pairs x 7 uvs incl. out-of-range, plus opacity 0 / negative / 1) -- 0 rows differ, 420 of 423 sampled real pixels",
      "slug": "alloc_free_texel",
      "speedup": 111.64516129032259,
      "theme": "composite the one sampled texel straight from the MString/float inputs instead of marshalling them through std::string vectors, nd::Array temporaries and nd::slice views"
    },
    {
      "compiled": true,
      "duration_s": 777.395,
      "fix_rounds": 0,
      "hypothesis": "At one layer the compute is a few microseconds of Maya handle traffic, so the two vector allocations, the MString copy and the four input reads the sampler never consumes when no layer loads (two of them moved compound plugs) are most of what sits above the output-write floor; the over arithmetic itself is negligible.",
      "index": 3,
      "ms": 0.0025,
      "note": "confirmed twice (incumbent under the noise floor): 0.002 / 0.003 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.15,
      "risk": "Reading uvCoord/wrapModeU/wrapModeV/borderColor lazily changes WHICH inputs a compute touches, never a value: the sampler is the only consumer and it runs only for a loaded layer. Never-set slots and empty paths are still weighted through the same double alpha-over step rather than skipped, so a NaN opacity poisons the stack exactly as the Python does.",
      "slug": "fuse_handles_lazy_reads",
      "speedup": 138.44,
      "theme": "Composite straight off the array handles -- no vector/MString marshalling -- and read uvCoord/wrapMode/borderColor only for a layer that actually loaded."
    },
    {
      "compiled": null,
      "duration_s": 619.61,
      "fix_rounds": 0,
      "hypothesis": "the bench seeds layers[0]='bench' (one unresolvable path) and 20000 opacities; preFilterRadius moves by 1e-3*k so its cache key rq=nearbyintf(r*10)/10 stays 0.0 and the thread_local memo hits every tick -- the only per-tick work is ~30 MDataBlock/MArrayDataHandle calls. Dropping the one redundant hL.next() after the last layer should shave one API call (~50 ns).",
      "index": 4,
      "ms": null,
      "note": "candidate is identical to the current best",
      "outcome": "no-change",
      "parity": null,
      "predicted_speedup": 1.02,
      "risk": "none taken: the single candidate (skip trailing MArrayDataHandle::next()) measured neutral-to-slower in 4 interleaved A/B pairs (base 0.0021-0.0023 ms vs cand 0.0022-0.0028 ms) and was reverted; a measurement-only probe with the entire layer loop disabled still took 0.0020-0.0021 ms vs 0.0021-0.0024 ms for the real compute, bounding every possible in-file gain at ~0.1 us. NOTE: ./bench.sh cannot run on this Windows host (build.sh is macOS-only: /Applications/Autodesk, clang -arch arm64 -bundle); all numbers above come from the identical harness invocation (benchmark_node.py, same --spec/--bench-array 20000/--bench-geo 400, --iters 9) against an MSVC /O2 build of this exact file, which also confirms the file compiles clean under cl.exe /std:c++17.",
      "slug": "noise_floor_no_change",
      "speedup": null,
      "theme": "compute() is ~0.1 us of a ~2.1 us pull; the rest is Maya plug evaluation + harness overhead, so no edit inside the file can move the number and the file is left byte-identical"
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (138.44x)",
  "rounds": 4,
  "speedup": 138.44,
  "stop_reason": "round 4 no-change -- nothing new to compound from",
  "type_name": "compositeTexture"
}
