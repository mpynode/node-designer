{
  "accepted": true,
  "baseline_ms": 4.7109,
  "bench": {
    "below_floor": true,
    "fingerprint": "skipped (node draws random numbers)",
    "floor_ms": 15.0,
    "perturbed": [
      "density (float)",
      "frame (time)",
      "height (int)",
      "uvCoord (float2)",
      "width (int)"
    ],
    "reason": "baseline 4.711 ms is below the 15 ms noise floor even at the largest bench scene (geo=bake array=4096); measured anyway -- every accept must clear 1.15x on two independent timings",
    "rung": [
      "bake",
      4096
    ],
    "small_rung": [
      "bake",
      1024
    ]
  },
  "best_ms": 1.8377,
  "created": 1789007222.9789095,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 4.7109,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 453.219,
      "fix_rounds": 0,
      "hypothesis": "the harness drives width = height = k (2..12), so the grid is under 150 cells and the per-frame cost was thread creation/join plus per-texel allocation overhead, not arithmetic; removing both takes the node side of the bake to the noise floor and leaves only ogsRender's fixed cost",
      "index": 1,
      "ms": 1.8377,
      "note": "confirmed twice (incumbent under the noise floor): 1.838 / 1.829 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.5,
      "risk": "the per-texel path is re-derived rather than shared with nd_texel, so any drift in the uv math would show up as a bake/compute mismatch; mitigated by keeping every cast, floor and the strict '<' clamp identical, and by keeping the single priming nd_texel call so seeding/stepping/bakePath writes still run through the ported code. Content key now hashes one byte per cell instead of 16 bytes per RGBA texel (same information, so the texture-manager cache behaves the same). Measured 4.52 -> 1.84 ms on this Windows box (baseline on entry 4.71); a diagnostic build that skipped the bake entirely measured 2.05 ms, so the remaining time is ogsRender itself. Note: build.sh is macOS-only and there is no clang here, so measurements used an MSVC build with the parent build.bat flags (/O2 /fp:precise) and the identical benchmark_node.py command.",
      "slug": "direct_board_bake",
      "speedup": 2.5634760842357296,
      "theme": "the VP2 bake sampled the board through the generic per-texel port (mutex, std::string, ~10 nd::Array temporaries per texel) and spawned min(12, h) threads per frame; it now reads the board buffer directly, serially, with the same float-narrowed uv -> floor -> truncate -> min-clamp -> >0.5 math"
    },
    {
      "compiled": true,
      "duration_s": 627.113,
      "fix_rounds": 0,
      "hypothesis": "nd_texel's prime call built a std::string, ~10 nd::Array temporaries (bit-packed vector<bool> via shared_ptr) and locked the mutex twice per frame; updateShader re-scanned shader.parameterList and re-acquired a sampler state every frame; removing all of it should shave ~0.1 ms off a ~1.9 ms ogsRender tick whose remainder is Maya's own render cost",
      "index": 2,
      "ms": 1.8445,
      "note": "",
      "outcome": "not-faster",
      "parity": "pass",
      "predicted_speedup": 1.06,
      "risk": "the seed/step/sample paths are hand-transcribed from the nd:: port -- MT19937 draw order (C order, double < double), zero-padded 3x3 sum, and the float2 narrowing + floor + truncating cast + min-clamp must stay identical; the bench noise (samples 1.7-2.6 ms) is larger than the expected win, so the measured 1.92 -> 1.80 ms (second run 1.90 ms) is directionally right but not statistically firm. Note: the workspace build.sh is the macOS clang script, so ./bench.sh cannot build on this Windows host; the measurements were taken with an equivalent MSVC cl /O2 /fp:precise build of the same file driven through the same benchmark_node.py command line.",
      "slug": "flat_board_no_nd_temporaries",
      "speedup": null,
      "theme": "the bench grid is k x k with k in the tens, so the frame cost is fixed overhead: store the board as a flat byte vector and seed/step/sample it directly, with one mutex acquisition, cached fragment parameter names and sampler state, and MPlug(attr) reads instead of six findPlug name lookups"
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (2.56x)",
  "rounds": 2,
  "speedup": 2.5634760842357296,
  "stop_reason": "round 2 not-faster -- nothing new to compound from",
  "type_name": "gameOfLifeTex"
}
