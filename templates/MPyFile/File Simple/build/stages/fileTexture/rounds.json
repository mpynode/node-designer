{
  "accepted": true,
  "baseline_ms": 127.9347,
  "bench": {
    "fingerprint": "checked (2 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "borderColor (color)",
      "brightness (float)",
      "contrast (float)",
      "preFilter (bool)",
      "preFilterRadius (float)",
      "uvCoord (float2)"
    ],
    "rung": [
      "bake",
      1024
    ]
  },
  "best_ms": 4.8314,
  "created": 1789007761.203833,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 127.9347,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 896.022,
      "fix_rounds": 0,
      "hypothesis": "at 1024x1024 the arithmetic is nothing -- per-texel allocation and per-bake thread creation are the whole cost, so hoisting everything that depends only on u or only on v, and keeping threads and the output buffer alive across bakes, leaves only the 16 MB GPU upload and the render",
      "index": 1,
      "ms": 5.2171,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 6.0,
      "risk": "bit-parity of the row kernel against nd_tex_sample: same taps, same `+ 0.0f`, same blend order, same == 0 strictness, and u/v narrowed to float exactly where the float2 shim did it; verified compute max diff 0.0 and a standalone exhaustive bit-compare of the row kernel and nd_texel_fast against the original per-texel expression (49,807,360 floats over 5 image/grid shapes x 5 brightness/contrast pairs, 0 mismatches). The pool must never be file-scope: cursor, generation and pending are per-override members, workers touch no Maya API, joined in the destructor.",
      "slug": "fused_bake_row_pool",
      "speedup": 24.522186655421596,
      "theme": "the VP2 bake paid an MString copy, two nd::Array heap temporaries and a memo probe per texel, then spawned 11 threads and zero-filled 16 MB every tick; resolve the buffer once, bake rows against a column table on a persistent pool into a reused buffer"
    },
    {
      "compiled": true,
      "duration_s": 732.453,
      "fix_rounds": 0,
      "hypothesis": "profiling the override showed bake ~0.5 ms, acquireTexture ~2.0 ms and ~2.9 ms of ogsRender overhead we cannot touch; swapping acquireTexture for an in-place update() alone was neutral (the cost is the 16 MB copy, not the allocation), so the remaining lever is to hide the bake under the upload by splitting the grid into 4 row bands and uploading band k-1 on the calling thread while workers bake band k",
      "index": 2,
      "ms": 4.8314,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.1,
      "risk": "region-update semantics (data pointer = band start, rowPitch = full row, Y range exclusive) were unverified in the SDK docs, so the 64px render PNG was byte-compared against the previous version (identical); any failed region update falls back to releasing the texture and a full-buffer acquireTexture; measured here on Windows/MSVC via an equivalent local build+bench because build.sh is macOS-only -- 5.43 ms -> 4.61 ms median, ogsRender overhead dominates what remains",
      "slug": "pipeline_bake_upload",
      "speedup": 26.479840211946847,
      "theme": "keep one persistent GPU texture per override and upload each baked band with MTexture::update(region) while the pool bakes the next band, so the 16 MB copy overlaps the bake instead of following it"
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (26.48x)",
  "rounds": 2,
  "speedup": 26.479840211946847,
  "stop_reason": "round 2 gained 1.08x, below the 1.15x needed to continue",
  "type_name": "fileTexture"
}
