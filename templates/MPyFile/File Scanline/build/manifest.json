{
  "manifest_version": 1,
  "porter_recipe_version": "24",
  "provider": "claude_cli",
  "model": "claude-opus-5[1m]",
  "strict": false,
  "plugin_name": "MPyFile_File_Scanline",
  "bundle": "compiled_templates/MPyFile/File Scanline/MPyFile_File_Scanline.bundle",
  "created": 1787962781.843988,
  "nodes": [
    {
      "source_node": "scanlineTex",
      "type_name": "scanlineTex",
      "type_id": "0x000543c2",
      "type_id_source": "derived",
      "base": "MPxNode",
      "spec_hash": "d38d6b40bbb9be970f77cf45a8d022232a4ec972758e1553aca2dce8b19e3436",
      "port_cache_key": "d38d6b40bbb9be970f77cf45a8d022232a4ec972758e1553aca2dce8b19e3436",
      "cache": "hit",
      "build_status": "compiled",
      "build_reason": "",
      "ported": false,
      "incomplete": [],
      "invented_io": [],
      "missing_includes": [],
      "vp2_skip": "",
      "verify": {
        "ran": true,
        "pass": true,
        "maxerr": null,
        "tol": 0.0,
        "reason": "reads an image file (MImage::readFromFile); output depends on external file state -- pointwise parity skipped (build verified to compile + load) | authored @maya_test: 1/1 passed",
        "timing": {
          "measured": false,
          "reason": "not timed -- generic parity did not pass for this node, so there is no trustworthy pair to measure (reads an image file (MImage::readFromFile); output depends on external file state -- pointwise parity skipped (build verified to compile + load))"
        },
        "authored_test": {
          "ran": true,
          "passed": true,
          "count": 1,
          "passes": 1,
          "reason": ""
        }
      },
      "spec": {
        "schema_version": 1,
        "source_node": "scanlineTex",
        "mpy_type": "mPyFile",
        "suggested": {
          "node_type_name": "scanlineTex",
          "class_name": "ScanlineTex",
          "type_id": "0x000764b6",
          "mpx_base": "MPxNode",
          "note": "texture/file evaluation node",
          "heaviness": "medium",
          "reads_image_file": true
        },
        "inputs": {
          "bands": {
            "type": "int",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "scalar",
              "fn": "MFnNumericAttribute",
              "data": "kInt",
              "cpp": "int",
              "read": "asInt",
              "portable": true
            },
            "min_value": 1,
            "default_value": 12
          },
          "speed": {
            "type": "float",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "scalar",
              "fn": "MFnNumericAttribute",
              "data": "kFloat",
              "cpp": "float",
              "read": "asFloat",
              "portable": true
            },
            "default_value": 0.1
          },
          "intensity": {
            "type": "float",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "scalar",
              "fn": "MFnNumericAttribute",
              "data": "kFloat",
              "cpp": "float",
              "read": "asFloat",
              "portable": true
            },
            "min_value": 0.0,
            "max_value": 1.0,
            "default_value": 0.6
          },
          "frame": {
            "type": "time",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "unit",
              "fn": "MFnUnitAttribute",
              "data": "kTime",
              "cpp": "double",
              "read": "asMTime",
              "portable": true
            }
          },
          "borderColor": {
            "type": "color",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "color",
              "fn": "MFnNumericAttribute",
              "data": "color",
              "cpp": "float[3]",
              "read": "asFloat3",
              "portable": true
            }
          },
          "colorSpace": {
            "type": "enum",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "enum",
              "fn": "MFnEnumAttribute",
              "data": "enum",
              "cpp": "short",
              "read": "asShort",
              "portable": true
            },
            "default_value": 0,
            "enum_names": [
              "[Texture] sRGB Encoded Rec.709 (sRGB)",
              "[Texture] Gamma 1.8 Encoded Rec.709",
              "[Texture] Gamma 2.2 Encoded Rec.709",
              "[Texture] Gamma 2.4 Encoded Rec.709",
              "[Texture] sRGB Encoded AP1",
              "[Texture] Gamma 2.2 Encoded AP1",
              "[Texture] sRGB Encoded P3-D65",
              "[Texture] Gamma 2.2 Encoded AdobeRGB",
              "[Scene-linear] scene-linear Rec.709-sRGB",
              "[Scene-linear] ACEScg",
              "[Scene-linear] ACES2065-1",
              "[Scene-linear] scene-linear DCI-P3 D65",
              "[Scene-linear] scene-linear AdobeRGB",
              "[Scene-linear] scene-linear Rec.2020",
              "[Display] sRGB",
              "[Display] Gamma 2.2 / Rec.709",
              "[Display] Rec.1886 / Rec.709 video",
              "[Display] AdobeRGB",
              "[Display] DCI-P3 D65",
              "[Utility] Raw",
              "[Log] ACEScct",
              "[Log] ARRI LogC v3 EI800 / AlexaWideGamut",
              "[Log] RED Log3G10 / REDWideGamutRGB",
              "[Log] Sony SLog3 / SGamut3",
              "[Log] Log film scan (ADX10)"
            ]
          },
          "fileName": {
            "type": "string",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "string",
              "fn": "MFnTypedAttribute",
              "data": "kString",
              "cpp": "MString",
              "read": "asString",
              "portable": true
            }
          },
          "preFilter": {
            "type": "bool",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "scalar",
              "fn": "MFnNumericAttribute",
              "data": "kBoolean",
              "cpp": "bool",
              "read": "asBool",
              "portable": true
            }
          },
          "preFilterKernel": {
            "type": "enum",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "enum",
              "fn": "MFnEnumAttribute",
              "data": "enum",
              "cpp": "short",
              "read": "asShort",
              "portable": true
            },
            "default_value": 3,
            "enum_names": [
              "Box",
              "Quadratic",
              "Quartic",
              "Gaussian"
            ]
          },
          "preFilterRadius": {
            "type": "float",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "scalar",
              "fn": "MFnNumericAttribute",
              "data": "kFloat",
              "cpp": "float",
              "read": "asFloat",
              "portable": true
            }
          },
          "uvCoord": {
            "type": "float2",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "vector2",
              "fn": "MFnNumericAttribute",
              "data": "k2Float",
              "cpp": "float[2]",
              "read": "asFloat2",
              "portable": true
            },
            "children": [
              "uCoord",
              "vCoord"
            ]
          },
          "wrapModeU": {
            "type": "enum",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "enum",
              "fn": "MFnEnumAttribute",
              "data": "enum",
              "cpp": "short",
              "read": "asShort",
              "portable": true
            },
            "default_value": 0,
            "enum_names": [
              "Wrap",
              "Clamp",
              "Mirror",
              "Border"
            ]
          },
          "wrapModeV": {
            "type": "enum",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "enum",
              "fn": "MFnEnumAttribute",
              "data": "enum",
              "cpp": "short",
              "read": "asShort",
              "portable": true
            },
            "default_value": 0,
            "enum_names": [
              "Wrap",
              "Clamp",
              "Mirror",
              "Border"
            ]
          }
        },
        "outputs": {
          "outAlpha": {
            "type": "float",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "scalar",
              "fn": "MFnNumericAttribute",
              "data": "kFloat",
              "cpp": "float",
              "read": "asFloat",
              "portable": true
            }
          },
          "outColor": {
            "type": "color",
            "is_array": false,
            "portable": true,
            "cpp": {
              "cat": "color",
              "fn": "MFnNumericAttribute",
              "data": "color",
              "cpp": "float[3]",
              "read": "asFloat3",
              "portable": true
            }
          }
        },
        "variables": {},
        "compute": "# Scanline file texture: sample the image through the FRAMEWORK, then\n# multiply by an animated horizontal scan band that scrolls with `frame`.\n#\n# The load is not this template's job -- self.read_texture() /\n# self.sample_texture() give it `fileName` decoded out of `colorSpace`, the\n# optional pre-filter and the wrap modes, the same as every other mPyFile.\n# What this template is ABOUT is the band below.\n#\n# `bands` = number of bands across V, `speed` = scroll rate, `intensity` = how\n# dark the troughs get (0 = flat, 1 = full black between).\nbuf = self.read_texture()\nv = self.uvCoord[1]\nr, g, b, a = self.sample_texture(buf, self.uvCoord[0], v)\n\nvv = v - np.floor(v)\ns = 0.5 + 0.5 * np.sin((vv * self.bands - self.frame * self.speed) * 2.0 * np.pi)\nscan = (1.0 - self.intensity) + self.intensity * s\n\nself.outColor = (r * scan, g * scan, b * scan)\nself.outAlpha = a\n",
        "init": "# ----------------------------------------------------------------------\n# File Simple -- Init tier.\n#\n# There is deliberately NO image-loading code here.\n#\n# Loading, colour-space decoding and pre-filtering are FRAMEWORK services.\n# Compute and Viewport reach them as self.read_texture() and\n# self.sample_texture() -- the \"M\" (method) rows in the Variables tab. They\n# honour every preset input an mPyFile already carries:\n#\n#   self.colorSpace       25 colour spaces (sRGB, ACEScct, LogC, S-Log3, ...)\n#   self.preFilter        smoothing on/off\n#   self.preFilterKernel  box / quadratic / quartic / gaussian\n#   self.preFilterRadius  smoothing radius\n#   self.wrapModeU/V      repeat / clamp / mirror / border\n#   self.borderColor      what a border-wrapped lookup returns off-image\n#   self.embeddedImage    baked image bytes read_texture() falls back to when\n#                         `fileName` is blank / missing, so a node ships\n#                         renderable with no file on disk\n#\n# So this template gets a stock Maya file node's full colour management for\n# free, and compiles to the same deterministic nd_tex_load_linear /\n# nd_tex_sample C++ kernels as the built-in default -- no AI port.\n#\n# numpy is imported because the Viewport tier uses it for the whole-image\n# brightness/contrast pass. The Init namespace is merged into the Compute and\n# Viewport namespaces before either runs, so anything defined here is in scope\n# for both.\n# ----------------------------------------------------------------------\nimport numpy as np\n",
        "affects": "all",
        "methods": "# Full setup for the scanline texture: create a plane + an UNLIT surfaceShader,\n# assign it, drive its colour from outColor, point fileName at the shipped\n# grid, set a playback range so the bands scroll on play, then (best effort)\n# build the Arnold OSL render path with tIn driven by time.\ndef demo(self):\n    from maya import cmds as mc\n    import os\n    name = self.get_name()\n\n    # surfaceShader, not lambert: the scanline modulation is a few percent at\n    # the bright end, and a lambert's diffuse + lighting term swamps exactly\n    # that signal. Unlit shows the buffer as computed.\n    plane = mc.polyPlane(width=10, height=10, subdivisionsX=1,\n                         subdivisionsY=1, name=name + \"_plane\")[0]\n    shader = mc.shadingNode(\"surfaceShader\", asShader=True,\n                            name=name + \"_surface\")\n    sg = mc.sets(renderable=True, noSurfaceShader=True, empty=True, name=shader + \"SG\")\n    mc.connectAttr(shader + \".outColor\", sg + \".surfaceShader\", force=True)\n    mc.sets(plane, edit=True, forceElement=sg)\n    mc.connectAttr(name + \".outColor\", shader + \".outColor\", force=True)\n\n    grid = None\n    try:\n        from mpynode._common.util.template_gallery import _bundled_templates_root\n        root = _bundled_templates_root()\n        if root:\n            cand = os.path.join(root, \"MPyFile\", \"File Scanline\", \"test_grid.png\")\n            if os.path.isfile(cand):\n                grid = cand\n    except Exception:\n        grid = None\n    if grid:\n        mc.setAttr(name + \".fileName\", grid, type=\"string\")\n\n    # Playback range so the scanline visibly scrolls on play.\n    try:\n        mc.playbackOptions(minTime=1, maxTime=120,\n                           animationStartTime=1, animationEndTime=120)\n        mc.currentTime(1)\n    except Exception:\n        pass\n\n    # Arnold OSL render path (best effort): drive tIn from time so it scrolls.\n    try:\n        from mpynode._common.osl.osl_targets import apply_osl_to_arnold\n        osl = apply_osl_to_arnold(name)\n        if osl:\n            if grid and mc.attributeQuery(\"fileName\", node=osl, exists=True):\n                mc.setAttr(osl + \".fileName\", grid, type=\"string\")\n            for p in (\"bands\", \"speed\", \"intensity\"):\n                if mc.attributeQuery(p, node=osl, exists=True):\n                    try:\n                        mc.setAttr(osl + \".\" + p, mc.getAttr(name + \".\" + p))\n                    except Exception:\n                        pass\n            if mc.attributeQuery(\"tIn\", node=osl, exists=True):\n                try:\n                    mc.connectAttr(\"time1.outTime\", osl + \".tIn\", force=True)\n                except Exception:\n                    pass\n            if mc.attributeQuery(\"aiSurfaceShader\", node=sg, exists=True):\n                amtl = mc.shadingNode(\"aiStandardSurface\", asShader=True,\n                                      name=name + \"_arnoldMtl\")\n                try:\n                    mc.setAttr(amtl + \".specular\", 0.0)\n                except Exception:\n                    pass\n                mc.connectAttr(osl + \".outColor\", amtl + \".baseColor\", force=True)\n                mc.connectAttr(amtl + \".outColor\", sg + \".aiSurfaceShader\", force=True)\n    except Exception:\n        pass\n\n    try:\n        mc.dgdirty(name + \".outColor\")\n        mc.getAttr(name + \".outColor\")\n    except Exception:\n        pass\n    return name\n\n\n@maya_test(label=\"Scanline: image sampled, scan band scrolls, magenta fallback\", digits=3)\ndef test_scanline(self):\n    \"\"\"Validate the node's INTENT (the SAME test passes on the interpreted node\n    and its C++ compile -> parity), mirroring the builder's own live checks:\n\n      1. A blank `fileName` yields the magenta \"no image\" hue.\n      2. With a real image wired in, the animated scan band scrolls: at a fixed\n         uv the `outColor` changes as `frame` (the timeline) advances.\n\n    Robust to a LIVE, demo-populated scene and to being run twice: the demo may\n    have wired `fileName`/`uvCoord`/scan params, so we break incoming\n    connections before driving each input.\n    \"\"\"\n    import os\n    import tempfile\n    from maya import cmds as mc\n    import maya.api.OpenMaya as om2\n    from mpynode._common.methods.test_helpers import assert_close, assert_true\n\n    name = self.get_name()\n\n    def _set(plug, *vals, **kw):\n        # A demo/user may have CONNECTED this input; break it so the test drives it.\n        for s in (mc.listConnections(plug, s=True, d=False, plugs=True) or []):\n            mc.disconnectAttr(s, plug)\n        mc.setAttr(plug, *vals, **kw)\n\n    def out_color(u, v, t):\n        _set(name + \".uCoord\", u)\n        _set(name + \".vCoord\", v)\n        mc.currentTime(t)\n        mc.dgdirty(name + \".outColor\")\n        return mc.getAttr(name + \".outColor\")[0]\n\n    # 1) Blank fileName -> the magenta \"no image\" sentinel. read_texture()\n    #    returns None and sample_texture() yields (1, 0, 1); the scan factor\n    #    then SCALES it (at intensity 0.6 the trough never reaches the rails),\n    #    so assert the HUE -- green off, red == blue, not black -- rather than\n    #    an exact (1, 0, 1).\n    _set(name + \".fileName\", \"\", type=\"string\")\n    miss = out_color(0.5, 0.3, 0.0)\n    assert_close(miss[1], 0.0)\n    assert_close(miss[0], miss[2])\n    assert_true(miss[0] > 0.01,\n                \"a missing image must read as magenta, not black\")\n\n    # Write a per-row gradient RGBA image (row r = grey r*30) so the V-sampling\n    # is actually exercised -- no external asset required.\n    w = h = 8\n    buf = bytearray()\n    for r in range(h):\n        val = r * 30\n        buf += bytes([val, val, val, 255]) * w\n    testimg = os.path.join(tempfile.gettempdir(), \"mpy_scanline_test.png\")\n    im = om2.MImage()\n    im.create(w, h, 4, om2.MImage.kByte)\n    im.setPixels(bytes(buf), w, h)\n    im.writeToFile(testimg, \"png\")\n\n    _set(name + \".fileName\", testimg, type=\"string\")\n    _set(name + \".bands\", 12)\n    _set(name + \".speed\", 0.1)\n    _set(name + \".intensity\", 0.6)\n\n    # The wrapper auto-wires `frame` to time1 on create, but a freshly built /\n    # deserialized node may not carry it -- ensure the timeline drives `frame`\n    # so currentTime actually scrolls the scan band.\n    if not (mc.listConnections(name + \".frame\", s=True, d=False) or []):\n        if not mc.objExists(\"time1\"):\n            mc.createNode(\"time\", name=\"time1\", skipSelect=True)\n        try:\n            mc.connectAttr(\"time1.outTime\", name + \".frame\", force=True)\n        except Exception:\n            pass\n\n    # 2) The scan band scrolls: at a fixed uv, some frame differs from the base\n    #    frame (avoid a single pair -- a phase shift of t*speed == integer lands\n    #    on the same scan value).\n    base = out_color(0.5, 0.3, 0.0)[0]\n    scrolled = any(\n        abs(out_color(0.5, 0.3, float(t))[0] - base) > 1e-3 for t in (5, 7, 13))\n    assert_true(scrolled,\n                \"scanline should scroll: outColor must change as frame advances\")\n\n\n\"\"\"Setup source for mPyFile nodes (self-first instance method).\"\"\"\n\n# VANILLA: this body ships as EMBEDDED PYTHON inside a compiled .mll and must run\n# on a machine with Maya but WITHOUT mpynode, so the setup_helpers ``_selection``\n# and the pure-cmds ``_api2.mpy_file.ensure_in_texture_list`` it used to import are\n# inlined below. They are FUNCTION-LOCAL rather than module-level because\n# node_setups.merge_type_default APPENDS this file to the host node's own methods\n# source -- a module-level def would clobber a same-named helper the host already\n# defines. ``warm_init_namespace`` is the one name that stays an import; see the\n# call site for why that is safe.\ndef setup(self, *args, **kwargs):\n    from maya import cmds as mc\n\n    def _selection(override=None, exclude=None):\n        \"\"\"Inputs for setup: `override` (the snapshot \"Run setup\" passes) wins\n        over the live selection; `exclude` drops this node so it is never its\n        own input.\"\"\"\n        sel = list(override) if override is not None else (\n            mc.ls(selection=True, long=False) or [])\n        return [n for n in sel if n != exclude] if exclude else sel\n\n    def _ensure_in_texture_list(n):\n        \"\"\"Wire ``n.message -> defaultTextureList1.textures`` so the Hypershade\n        can graph the node (parity with ``shadingNode -asTexture``).\n\n        Idempotent AND self-healing: it inspects the texture-list's source plugs\n        so it is robust to the create-time race where ``shadingNode -asTexture``\n        ALSO wires this connection -- duplicates are disconnected so exactly one\n        remains. Vendored from _api2.mpy_file.ensure_in_texture_list, which is\n        pure maya.cmds and works against a compiled node too -- importing it\n        would drag mpynode into a compiled bundle.\"\"\"\n        try:\n            if not mc.objExists(n):\n                return False\n            msg = n + \".message\"\n            if not mc.objExists(\"defaultTextureList1\"):\n                # Default node is created on demand by shadingNode; make it so\n                # the connection has a destination even in a minimal scene.\n                try:\n                    mc.createNode(\"defaultTextureList\",\n                                  name=\"defaultTextureList1\", shared=True)\n                except Exception:\n                    pass\n            pairs = mc.listConnections(\n                \"defaultTextureList1.textures\", connections=True, plugs=True,\n                source=True, destination=False) or []\n            mine = [pairs[i] for i in range(0, len(pairs) - 1, 2)\n                    if pairs[i + 1] == msg]\n            if mine:\n                for extra in mine[1:]:\n                    try:\n                        mc.disconnectAttr(msg, extra)\n                    except Exception:\n                        pass\n                return True\n            mc.connectAttr(msg, \"defaultTextureList1.textures\",\n                           nextAvailable=True)\n            return True\n        except Exception:\n            return False\n\n    name = self.get_name()\n    # ``self`` is the already-created mPyFile (create() ran shadingNode -asTexture,\n    # wired time1, seeded Init/Compute/Viewport + a place2dTexture). Wire outColor\n    # on top of that plumbing -- do NOT re-create place2dTexture or re-seed.\n    _ensure_in_texture_list(name)\n    # INTERPRETED-ONLY, and deliberately left as an import rather than vendored:\n    # it registers the node's Init source into mpynode's init registry, and it\n    # gates on the ``_initSource`` plug -- which a compiled node does not have\n    # (command_dispatch.COMPILED_UNSUPPORTED), so a vendored copy could only ever\n    # return False in a bundle. Guarded so a missing mpynode skips it instead of\n    # killing the whole setup; the function itself never raises.\n    try:\n        from mpynode._api2.mpy_file import warm_init_namespace\n        warm_init_namespace(name)\n    except Exception:\n        pass\n\n    # Selection snapshot (build()/Run setup pass it via kwargs[\"selection\"]);\n    # exclude self so it is never treated as its own shader input.\n    def _is_surface_shader(n):\n        cls_tags = mc.getClassification(mc.nodeType(n)) or []\n        return any(\"shader/surface\" in t for t in cls_tags)\n    sel = [s for s in _selection(override=kwargs.get(\"selection\"),\n                                 exclude=name) if _is_surface_shader(s)]\n\n    if sel:\n        shader = sel[-1]\n        dest = next((a for a in (\"color\", \"baseColor\")\n                     if mc.attributeQuery(a, node=shader, exists=True)), None)\n        if dest:\n            mc.connectAttr(name + \".outColor\", \"%s.%s\" % (shader, dest), force=True)\n    return name\n",
        "commands": [],
        "portability": {
          "portable": true,
          "blockers": [],
          "warnings": [
            "reads an image file (self.read_texture) -> ported to MImage::readFromFile; output depends on external file state, so pointwise parity is loose/skipped (not bit-identical to PIL/cv2)",
            "uses numpy -- vectorized math is reverse-engineered to C++ loops (verify numerically; np.random maps to C++ <random>; file I/O lowers only where the dtype is explicit -- np.fromfile/np.save/.tofile and ndio.read/ndio.write -- while np.load/np.loadtxt are blockers)"
          ],
          "unported": [],
          "reads_image_file": true,
          "reads_embedded_image": true,
          "uses_mesh_intersector": false
        }
      }
    }
  ]
}
