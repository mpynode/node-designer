{
  "accepted": true,
  "baseline_ms": 0.0093,
  "bench": {
    "below_floor": true,
    "fingerprint": "checked (2 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "bands (int)",
      "borderColor (color)",
      "frame (time)",
      "intensity (float)",
      "preFilter (bool)",
      "preFilterRadius (float)",
      "speed (float)",
      "uvCoord (float2)"
    ],
    "reason": "baseline 0.009 ms is below the 15 ms noise floor even at the largest bench scene (geo=400 array=20000); measured anyway -- every accept must clear 1.15x on two independent timings",
    "rung": [
      400,
      20000
    ],
    "small_rung": [
      40,
      512
    ]
  },
  "best_ms": 0.0032,
  "created": 1788992594.715808,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 0.0093,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 974.133,
      "fix_rounds": 0,
      "hypothesis": "at 6 us per evaluation the node is pure per-call overhead: ~24 malloc/free pairs from nd::from_data/slice/sub/floor/mul/add/sin dominate, then two MString copies and a second texture-cache probe for outSize; nd::sin and nd::floor are std::sin/std::floor on double so a scalar rewrite is bit-identical under /fp:precise and -ffp-contract=off",
      "index": 1,
      "ms": 0.0032,
      "note": "confirmed twice (incumbent under the noise floor): 0.003 / 0.003 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.8,
      "risk": "measured on Windows/MSVC via a twin of bench.sh (the generated build.sh is macOS-only and cannot run on this host): 0.0063 ms -> 0.0028 ms (2.2x), bit-exact against the untouched original over 400 seeded random input sets (all wrap modes, prefilter kernels, colour spaces, blank fileName). Diagnostic floors: the same compute with the texel work deleted measures 0.0026 ms and with no input reads 0.0020 ms, so what remains is Maya datablock access and the harness's own pull overhead; the only lever left (skipping reads of inputs that hold via dirty tracking) has no cheap fingerprint backstop and was deliberately not taken. Round 2 (const MString& binding, single cache probe, no redundant pre-zero writes) is inside the noise but consistently 0.1-0.2 us lower.",
      "slug": "scalar_texel",
      "speedup": 2.9062499999999996,
      "theme": "the ported texel body built about a dozen heap-allocating nd::Array<double> temporaries to do scalar arithmetic on one pixel; the same expression on plain doubles, in the same operation order, removes every allocation from compute()"
    },
    {
      "compiled": null,
      "duration_s": 556.411,
      "fix_rounds": 0,
      "hypothesis": "compute() is one texel: 13 inputValue reads, one memoised null-texture probe, one sin, four output writes. The only Maya-API work not needed for the answer was the four-way MPlug::operator!= gate, each call re-resolving the plug's attribute. One MObject fetch plus pointer compares should shave ~100-200 ns off a ~2.5-3 us tick. No algorithmic lever exists: the node is elementwise over ONE sample, the texture cache is already per-instance and keyed on every input it depends on, and the bench leaves fileName blank so the sample path is the null-texture sentinel.",
      "index": 2,
      "ms": null,
      "note": "candidate is identical to the current best",
      "outcome": "no-change",
      "parity": null,
      "predicted_speedup": 1.04,
      "risk": "The gain is inside the harness noise (run-to-run medians on the same binary spanned 0.0025-0.0054 ms). Interleaved measurement: original 0.0025 ms; change 0.0029 / 0.0027 / 0.0030 ms -- not lower, so reverted per the keep-if-lower rule. HARNESS NOTE: the workspace build.sh is the macOS clang recipe (looks under /Applications/Autodesk, -arch arm64 -bundle, lipo) and bench.sh's PYTHONPATH/MAYA_PLUG_IN_PATH use ':' separators, so ./bench.sh cannot run on this Windows host; every number above came from an equivalent scratch script that compiles the SAME file with the gate's own MSVC flags (toolchain._MSVC_CXXFLAGS + _msvc_defines, output byte-size 114688 matching the engine's .mll) and runs the SAME benchmark_node.py with the SAME --spec/--bench-array/--bench-geo/--iters arguments. build.sh and bench.sh were not edited.",
      "slug": "single_attribute_plug_gate",
      "speedup": null,
      "theme": "resolve plug.attribute() once and compare MObjects instead of four MPlug-vs-MObject comparisons at the top of compute(); measured as noise, reverted, file left identical to the entry version"
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (2.91x)",
  "rounds": 2,
  "speedup": 2.9062499999999996,
  "stop_reason": "round 2 no-change -- nothing new to compound from",
  "type_name": "scanlineTex"
}
