// mPyMega -- combined MPyNode plugin entry (generated by bundler.py).
// Registers 37 node(s): mPyDnet, comboCorrectives, procrustesTags, nurbsWave, patchRelax, rbfWrapDeformer, sineRipple, unitSphereCollision, compositeTexture, scanlineTex, fileTexture, gameOfLifeTex, twoBoneIK, animatedSelection, animatedText, meshRegionLocator, widgetShowcase, diskMeshCache, gameOfLifeMesh, jsonMeshReader, meshMaze, metaballs, uvLayoutMesh, voxelizeMesh, bubbleSort, hexAttribute, ouch, rbfWrap, spine, spline, springChain, helixCurve, rippleSurf, dualQuaternionSkin, linearBlendSkin, twistSwingSkin, aimTransform
#include <maya/MFnPlugin.h>
#include <maya/MObject.h>
#include <maya/MStatus.h>

MStatus register_MPyDnet(MFnPlugin&); MStatus deregister_MPyDnet(MFnPlugin&);
MStatus register_ComboCorrectives(MFnPlugin&); MStatus deregister_ComboCorrectives(MFnPlugin&);
MStatus register_ProcrustesTags(MFnPlugin&); MStatus deregister_ProcrustesTags(MFnPlugin&);
MStatus register_NurbsWave(MFnPlugin&); MStatus deregister_NurbsWave(MFnPlugin&);
MStatus register_PatchRelax(MFnPlugin&); MStatus deregister_PatchRelax(MFnPlugin&);
MStatus register_RbfWrapDeformer(MFnPlugin&); MStatus deregister_RbfWrapDeformer(MFnPlugin&);
MStatus register_SineRipple(MFnPlugin&); MStatus deregister_SineRipple(MFnPlugin&);
MStatus register_UnitSphereCollision(MFnPlugin&); MStatus deregister_UnitSphereCollision(MFnPlugin&);
MStatus register_CompositeTexture(MFnPlugin&); MStatus deregister_CompositeTexture(MFnPlugin&);
MStatus register_ScanlineTex(MFnPlugin&); MStatus deregister_ScanlineTex(MFnPlugin&);
MStatus register_FileTexture(MFnPlugin&); MStatus deregister_FileTexture(MFnPlugin&);
MStatus register_GameOfLifeTex(MFnPlugin&); MStatus deregister_GameOfLifeTex(MFnPlugin&);
MStatus register_TwoBoneIK(MFnPlugin&); MStatus deregister_TwoBoneIK(MFnPlugin&);
MStatus register_AnimatedSelection(MFnPlugin&); MStatus deregister_AnimatedSelection(MFnPlugin&);
MStatus register_AnimatedText(MFnPlugin&); MStatus deregister_AnimatedText(MFnPlugin&);
MStatus register_MeshRegionLocator(MFnPlugin&); MStatus deregister_MeshRegionLocator(MFnPlugin&);
MStatus register_WidgetShowcase(MFnPlugin&); MStatus deregister_WidgetShowcase(MFnPlugin&);
MStatus register_DiskMeshCache(MFnPlugin&); MStatus deregister_DiskMeshCache(MFnPlugin&);
MStatus register_GameOfLifeMesh(MFnPlugin&); MStatus deregister_GameOfLifeMesh(MFnPlugin&);
MStatus register_JsonMeshReader(MFnPlugin&); MStatus deregister_JsonMeshReader(MFnPlugin&);
MStatus register_MeshMaze(MFnPlugin&); MStatus deregister_MeshMaze(MFnPlugin&);
MStatus register_Metaballs(MFnPlugin&); MStatus deregister_Metaballs(MFnPlugin&);
MStatus register_UvLayoutMesh(MFnPlugin&); MStatus deregister_UvLayoutMesh(MFnPlugin&);
MStatus register_VoxelizeMesh(MFnPlugin&); MStatus deregister_VoxelizeMesh(MFnPlugin&);
MStatus register_BubbleSort(MFnPlugin&); MStatus deregister_BubbleSort(MFnPlugin&);
MStatus register_HexAttribute(MFnPlugin&); MStatus deregister_HexAttribute(MFnPlugin&);
MStatus register_Ouch(MFnPlugin&); MStatus deregister_Ouch(MFnPlugin&);
MStatus register_RbfWrap(MFnPlugin&); MStatus deregister_RbfWrap(MFnPlugin&);
MStatus register_Spine(MFnPlugin&); MStatus deregister_Spine(MFnPlugin&);
MStatus register_Spline(MFnPlugin&); MStatus deregister_Spline(MFnPlugin&);
MStatus register_SpringChain(MFnPlugin&); MStatus deregister_SpringChain(MFnPlugin&);
MStatus register_HelixCurve(MFnPlugin&); MStatus deregister_HelixCurve(MFnPlugin&);
MStatus register_RippleSurf(MFnPlugin&); MStatus deregister_RippleSurf(MFnPlugin&);
MStatus register_DualQuaternionSkin(MFnPlugin&); MStatus deregister_DualQuaternionSkin(MFnPlugin&);
MStatus register_LinearBlendSkin(MFnPlugin&); MStatus deregister_LinearBlendSkin(MFnPlugin&);
MStatus register_TwistSwingSkin(MFnPlugin&); MStatus deregister_TwistSwingSkin(MFnPlugin&);
MStatus register_AimTransform(MFnPlugin&); MStatus deregister_AimTransform(MFnPlugin&);

MStatus initializePlugin(MObject obj) {
    MFnPlugin plugin(obj, "mpynode-native", "1.0", "Any");
    MStatus st;
    st = register_MPyDnet(plugin);
    if (!st) { st.perror("register mPyDnet"); return st; }
    st = register_ComboCorrectives(plugin);
    if (!st) { st.perror("register comboCorrectives"); return st; }
    st = register_ProcrustesTags(plugin);
    if (!st) { st.perror("register procrustesTags"); return st; }
    st = register_NurbsWave(plugin);
    if (!st) { st.perror("register nurbsWave"); return st; }
    st = register_PatchRelax(plugin);
    if (!st) { st.perror("register patchRelax"); return st; }
    st = register_RbfWrapDeformer(plugin);
    if (!st) { st.perror("register rbfWrapDeformer"); return st; }
    st = register_SineRipple(plugin);
    if (!st) { st.perror("register sineRipple"); return st; }
    st = register_UnitSphereCollision(plugin);
    if (!st) { st.perror("register unitSphereCollision"); return st; }
    st = register_CompositeTexture(plugin);
    if (!st) { st.perror("register compositeTexture"); return st; }
    st = register_ScanlineTex(plugin);
    if (!st) { st.perror("register scanlineTex"); return st; }
    st = register_FileTexture(plugin);
    if (!st) { st.perror("register fileTexture"); return st; }
    st = register_GameOfLifeTex(plugin);
    if (!st) { st.perror("register gameOfLifeTex"); return st; }
    st = register_TwoBoneIK(plugin);
    if (!st) { st.perror("register twoBoneIK"); return st; }
    st = register_AnimatedSelection(plugin);
    if (!st) { st.perror("register animatedSelection"); return st; }
    st = register_AnimatedText(plugin);
    if (!st) { st.perror("register animatedText"); return st; }
    st = register_MeshRegionLocator(plugin);
    if (!st) { st.perror("register meshRegionLocator"); return st; }
    st = register_WidgetShowcase(plugin);
    if (!st) { st.perror("register widgetShowcase"); return st; }
    st = register_DiskMeshCache(plugin);
    if (!st) { st.perror("register diskMeshCache"); return st; }
    st = register_GameOfLifeMesh(plugin);
    if (!st) { st.perror("register gameOfLifeMesh"); return st; }
    st = register_JsonMeshReader(plugin);
    if (!st) { st.perror("register jsonMeshReader"); return st; }
    st = register_MeshMaze(plugin);
    if (!st) { st.perror("register meshMaze"); return st; }
    st = register_Metaballs(plugin);
    if (!st) { st.perror("register metaballs"); return st; }
    st = register_UvLayoutMesh(plugin);
    if (!st) { st.perror("register uvLayoutMesh"); return st; }
    st = register_VoxelizeMesh(plugin);
    if (!st) { st.perror("register voxelizeMesh"); return st; }
    st = register_BubbleSort(plugin);
    if (!st) { st.perror("register bubbleSort"); return st; }
    st = register_HexAttribute(plugin);
    if (!st) { st.perror("register hexAttribute"); return st; }
    st = register_Ouch(plugin);
    if (!st) { st.perror("register ouch"); return st; }
    st = register_RbfWrap(plugin);
    if (!st) { st.perror("register rbfWrap"); return st; }
    st = register_Spine(plugin);
    if (!st) { st.perror("register spine"); return st; }
    st = register_Spline(plugin);
    if (!st) { st.perror("register spline"); return st; }
    st = register_SpringChain(plugin);
    if (!st) { st.perror("register springChain"); return st; }
    st = register_HelixCurve(plugin);
    if (!st) { st.perror("register helixCurve"); return st; }
    st = register_RippleSurf(plugin);
    if (!st) { st.perror("register rippleSurf"); return st; }
    st = register_DualQuaternionSkin(plugin);
    if (!st) { st.perror("register dualQuaternionSkin"); return st; }
    st = register_LinearBlendSkin(plugin);
    if (!st) { st.perror("register linearBlendSkin"); return st; }
    st = register_TwistSwingSkin(plugin);
    if (!st) { st.perror("register twistSwingSkin"); return st; }
    st = register_AimTransform(plugin);
    if (!st) { st.perror("register aimTransform"); return st; }
    return MS::kSuccess;
}

MStatus uninitializePlugin(MObject obj) {
    MFnPlugin plugin(obj);
    deregister_AimTransform(plugin);
    deregister_TwistSwingSkin(plugin);
    deregister_LinearBlendSkin(plugin);
    deregister_DualQuaternionSkin(plugin);
    deregister_RippleSurf(plugin);
    deregister_HelixCurve(plugin);
    deregister_SpringChain(plugin);
    deregister_Spline(plugin);
    deregister_Spine(plugin);
    deregister_RbfWrap(plugin);
    deregister_Ouch(plugin);
    deregister_HexAttribute(plugin);
    deregister_BubbleSort(plugin);
    deregister_VoxelizeMesh(plugin);
    deregister_UvLayoutMesh(plugin);
    deregister_Metaballs(plugin);
    deregister_MeshMaze(plugin);
    deregister_JsonMeshReader(plugin);
    deregister_GameOfLifeMesh(plugin);
    deregister_DiskMeshCache(plugin);
    deregister_WidgetShowcase(plugin);
    deregister_MeshRegionLocator(plugin);
    deregister_AnimatedText(plugin);
    deregister_AnimatedSelection(plugin);
    deregister_TwoBoneIK(plugin);
    deregister_GameOfLifeTex(plugin);
    deregister_FileTexture(plugin);
    deregister_ScanlineTex(plugin);
    deregister_CompositeTexture(plugin);
    deregister_UnitSphereCollision(plugin);
    deregister_SineRipple(plugin);
    deregister_RbfWrapDeformer(plugin);
    deregister_PatchRelax(plugin);
    deregister_NurbsWave(plugin);
    deregister_ProcrustesTags(plugin);
    deregister_ComboCorrectives(plugin);
    deregister_MPyDnet(plugin);
    return MS::kSuccess;
}
