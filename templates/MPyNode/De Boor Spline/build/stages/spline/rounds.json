{
  "accepted": true,
  "baseline_ms": 94.0015,
  "bench": {
    "fingerprint": "checked (1 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "cv[0] (vector)"
    ],
    "rung": [
      40,
      512
    ]
  },
  "best_ms": 0.0153,
  "created": 1790268685.2850606,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 94.0015,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 471.607,
      "fix_rounds": 0,
      "hypothesis": "The weights depend only on (c, degree, n), and all three hold between ticks; only cv VALUES move. Outside the support [jmin-d, jmax] every weight is exactly +-0, and acc is never -0, so skipping those terms is bit-exact when cvs are finite. Non-finite cvs, n<2 and negative degree fall back to the original lowered loop, kept unchanged.",
      "index": 1,
      "ms": 0.0153,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 200.0,
      "risk": "Exactness depends on the +-0 argument: finite coefficients (denom >= 1, u finite when n >= 2) and finite cvs, so any non-finite cv takes the original path. Weights are computed with the recursion's exact per-node arithmetic, memoised as a triangle. The cache is a pure function of its complete integer key; the setDependentsDirty/preEvaluation invalidation on degree is a backstop. In-place output write happens only when the array already holds exactly logical indices 0..n-1, otherwise the MArrayDataBuilder path runs.",
      "slug": "cache_sparse_basis",
      "speedup": 6143.888888888889,
      "theme": "Cache the De Boor basis weights per instance as a sparse table (at most degree+1 per sample), keyed on cv count, effective degree and sample count, so a tick that only moves cvs costs O(n*(d+1)) instead of O(n*c*2^(d+1)) recursive std::function calls; then write the output array in place instead of rebuilding it with MArrayDataBuilder."
    },
    {
      "compiled": true,
      "duration_s": 1066.565,
      "fix_rounds": 0,
      "hypothesis": "the sparse basis cache already made the math ~1 us, so compute is bound by ~3.5k OpenMaya calls on 512 cvs + 512 samples (profiled: read 4.8 us, basis eval 1.3 us, write+finalize 7.8 us); dropping one call per element on each side and the staging pass removes a level of per-element work while the arithmetic (same operand order, same +-0 behaviour) is untouched",
      "index": 2,
      "ms": 0.0203,
      "note": "",
      "outcome": "not-faster",
      "parity": "pass",
      "predicted_speedup": 1.25,
      "risk": "the dense proof assumes MArrayDataHandle physical order is ascending distinct logical order (last physical at logical n-1 => indices are exactly 0..n-1); any other layout falls back to the original per-element elementIndex loops. Folded finiteness test uses (x - x) == 0.0, exact iff finite under -ffp-contract=off / fp:precise.",
      "slug": "direct_handle_io",
      "speedup": null,
      "theme": "cut per-element Maya data-handle calls: prove the cv and samples arrays dense once instead of calling elementIndex per element, write outputs through the asDouble3 reference instead of set3Double, and evaluate each sample straight into its output handle instead of staging a 12 KB buffer"
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (6143.89x)",
  "rounds": 2,
  "speedup": 6143.888888888889,
  "stop_reason": "round 2 not-faster -- nothing new to compound from",
  "type_name": "spline"
}
