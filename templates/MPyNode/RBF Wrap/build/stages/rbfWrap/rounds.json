{
  "accepted": true,
  "baseline_ms": 5213.128,
  "bench": {
    "fingerprint": "checked (1 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "deformCage <- pSphereShape1.vtx[0]",
      "geoToDeform <- pSphereShape2.vtx[0]"
    ],
    "rung": [
      40,
      512
    ]
  },
  "best_ms": 0.477,
  "created": 1789000986.3362968,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 5213.128,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 882.178,
      "fix_rounds": 0,
      "hypothesis": "the 1566^3 Gauss-Jordan inverse is >95% of the 5.2 s and is rebuilt for nothing every tick; a whole-node dgdirty raises setDependentsDirty on every plug, so the DG/EM flags must trigger a key re-validation (18 KB memcmp) rather than an unconditional rebuild or the cache never hits",
      "index": 1,
      "ms": 0.9917,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 60.0,
      "risk": "a cache keyed on a geometry input is only exercised by the GEO parity path, so the key is the FULL float buffer compared byte-for-byte (not pointer+count); the fused kernel and both threaded maps keep nd::matmul's ascending-l accumulation with one independent chain per output column so the bits match the serial path; the pool holds its chunk cursor as a per-node member, blocks on a condvar, and is joined in the destructor",
      "slug": "cache_rest_inverse",
      "speedup": 5256.759100534436,
      "theme": "inv(A) depends only on restCage, which holds between ticks, so cache it per instance on a byte key of the rest points; then cache the Nn x M kernel row-wise on each P row and run the two remaining 7.3M-MAC row loops on a persistent pool"
    },
    {
      "compiled": true,
      "duration_s": 1002.655,
      "fix_rounds": 0,
      "hypothesis": "The two per-tick passes each stream a ~19.6 MB matrix (Ainv for W, Ke for the output) and together exceed the 36 MB L3, so the node is bandwidth-bound; a T row change at index k only invalidates chain partials at l <= k, so a checkpointed descending replay removes the Ainv stream entirely for the bench's moved vtx[0] while staying bit-identical to a full replay of the same chain. Follow-ups in the same round: reuse the output mesh via setPoints when topology is byte-identical (MFnMesh::create was ~240 us of a ~900 us compute), read topology once with MIntArray::get, run the short W replay serially.",
      "index": 2,
      "ms": 0.8155,
      "note": "confirmed twice (incumbent under 2 ms): 0.816 / 0.749 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.6,
      "risk": "Descending accumulation is a different (deterministic, thread-count-independent) summation order than the previous ascending chain, so W differs from the old build by rounding noise; both are within the tolerance gate against numpy's BLAS order. Output mesh reuse mutates the datablock's previous kMeshData in place, guarded on MObject identity and vertex/polygon/face-vertex counts; falls back to MFnMesh::create on any mismatch.",
      "slug": "checkpointed_w_cache",
      "speedup": 6392.554261189454,
      "theme": "W = Ainv @ T is accumulated in descending order with the partial sum checkpointed every 64 terms, so one moved cage vertex replays a 64-column strip of Ainv instead of streaming the whole 20 MB inverse every tick."
    },
    {
      "compiled": true,
      "duration_s": 1168.094,
      "fix_rounds": 0,
      "hypothesis": "The hot H@W kernel measured 110-140 us in a standalone microbench but 300-500 us inside Maya, and the same kernel slowed to 400-600 us in the microbench once a 700 us sleep separated reps -- so the pool was paying condvar wake-up latency on 15 sleeping threads every tick, not memory bandwidth. The W replay read 64 scattered doubles from each of 1566 rows of a 20 MB row-major inverse (300 us for 100k mul-adds); iterating l-outer over Ainv^T turns that into 64 contiguous 12.5 KB rows against L1-resident SoA accumulators. Reuse never fired (reused=0 every tick, why=7): asMesh() returns a different kMeshData wrapper than nd_build_mesh's, so a 200-280 us MFnMesh::create ran every tick.",
      "index": 3,
      "ms": 0.477,
      "note": "confirmed twice (incumbent under 2 ms): 0.477 / 0.453 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 1.6,
      "risk": "The prewake is a bounded yield-spin (<= RBFWRAP_SPIN_US, default 400 us, only after a prewake and never after a job) so under heavy load 15 workers can burn up to 400 us each per compute before blocking; the transposed inverse is a second 20 MB table per node (the row-major one is no longer kept); reuse now mutates the datablock's own mesh object in place via setPoints, guarded on full counts/indices equality plus the object's own vertex/polygon/face-vertex counts",
      "slug": "prewake_pool_stream_replay",
      "speedup": 10928.989517819706,
      "theme": "Profile every per-tick phase in Maya, then remove the latency each one was paying for nothing: the W replay now streams contiguous rows of a transposed inverse, the row pool is woken at compute() entry so its OS wake-up overlaps the serial phases, and the output-mesh reuse finally fires because identity is keyed on the object the datablock hands back rather than the one passed to setMObject."
    },
    {
      "compiled": true,
      "duration_s": 1352.093,
      "fix_rounds": 0,
      "hypothesis": "Phase timers showed the reuse path spending 65-80 us before setPoints (8 us) and only ~4 us in the vector compares, so the MFnMesh construction plus numFaceVertices() was the cost; binding once and dropping the face-vertex query removes it. Bundled with three smaller serial cuts measured in the same timers: W-replay checkpoint stride 16 -> 4 (30 -> 10 us for a moved cage vertex), raw float inputs widened on use instead of three push_back double copies plus nd::sum_mul (25 -> 3 us), and the MPoint intermediate replaced by a direct float4 buffer handed to MFloatPointArray in one bulk copy (10 us). A fifth attempt -- persistent MFnMesh for the three INPUT meshes -- was predicted to save another ~40 us and instead regressed the first input pull from ~55 to ~100 us and the bench median to 0.68 ms: holding a reference to an upstream data object stops Maya recycling it, so every tick pays a fresh allocation upstream. Reverted. A sixth attempt -- pool chunk 32 -> 8 rows to shorten the E-core straggler tail in the H@W region -- was predicted at 1.05x and measured 0.58 vs 0.40 ms; reverted as well (noise or real, it did not go down).",
      "index": 4,
      "ms": 0.5007,
      "note": "",
      "outcome": "not-faster",
      "parity": "pass",
      "predicted_speedup": 1.25,
      "risk": "Bench medians on this Windows box swing 0.42-0.68 ms between identical builds because the 16-thread H@W region varies 80-690 us tick to tick (8P+16E Arrow Lake scheduling), so the serial savings (~100 us of compute() per the phase timers) may not show in a single median; correctness risk is nil -- every arithmetic chain and every (double)float / (float)double narrowing is byte-identical to the previous revision.",
      "slug": "persistent_output_fnset",
      "speedup": null,
      "theme": "Keep one MFnMesh bound to the reused output mesh across evaluations, because MFnMesh::setObject on a mesh data object costs ~50 us per tick -- more than setPoints itself -- and the identity check already proves the binding is current."
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (10928.99x)",
  "rounds": 4,
  "speedup": 10928.989517819706,
  "stop_reason": "round 4 not-faster -- nothing new to compound from",
  "type_name": "rbfWrap"
}
