{
  "accepted": true,
  "baseline_ms": 4921.7328,
  "bench": {
    "fingerprint": "checked (1 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "deformCage <- pSphereShape2.vtx[0]",
      "input[0].inputGeometry <- pSphereShape1Orig.vtx[0]"
    ],
    "rung": [
      40,
      512
    ]
  },
  "best_ms": 0.7439,
  "created": 1788991031.5430675,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 4921.7328,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 905.637,
      "fix_rounds": 0,
      "hypothesis": "the whole 4.9 s is the O(M^3) nd::inv of a 1566x1566 system that depends only on restCage, which HOLDS between ticks; deformCage and one input vertex move, so per tick only W = Ainv@T (7.4 MFLOP), one H row (1562 logs) and warped = H@W (7.3 MFLOP) need recomputing; every accumulation keeps nd::matmul/sum_mul's order (acc starts at 0, ascending terms, a*b operand order), so the result is bit-identical to the unoptimised lowering",
      "index": 1,
      "ms": 0.7439,
      "note": "",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 500.0,
      "risk": "invalidation of the inverse relies on a full-content byte key of the rest points (a complete fingerprint of everything Ainv depends on); the setDependentsDirty/preEvaluation hooks are wired but can only force the same re-key the compare already does. bench.sh could not run on this Windows host (build.sh is the macOS clang script), so the numbers come from an equivalent MSVC build + benchmark_node.py invocation, and the gate's own re-measure is the authority.",
      "slug": "cache_inverse_thread_rows",
      "speedup": 6616.121521709907,
      "theme": "cache the (M+4)x(M+4) Gauss-Jordan inverse on the rest cage's bytes, cache the evaluation matrix H row-by-row on each point's bytes, and run the two remaining per-row matmuls (Ainv@T, H@W) as a deterministic parallel map on a persistent per-node pool"
    },
    {
      "compiled": null,
      "duration_s": 1284.481,
      "fix_rounds": 0,
      "hypothesis": "at M = Nn = 1562 each pass streams a 19.6 MB double matrix; 24 threads reach ~80 GB/s (DDR5 peak: 1 thread 3.57 ms, 8 threads 1.11 ms, 24 threads 0.625 ms), so the passes are DRAM-bound and the remaining slack is the second condvar wake of 23 workers (~50-100 us) plus its tail; one wake per tick should recover ~10%. A second idea -- stable per-participant row ownership so each core re-reads the same slice of Ainv/H from its L2/L3 across ticks -- was measured first and came out neutral (0.625 vs 0.6246 ms): the 39 MB cyclic footprint exceeds the 36 MB L3, so nothing survives a tick",
      "index": 2,
      "ms": null,
      "note": "candidate is identical to the current best",
      "outcome": "no-change",
      "parity": null,
      "predicted_speedup": 1.1,
      "risk": "participants spinning at the phase barrier compete with the stragglers still finishing phase 1; measured 0.773 ms with yield()-spin and 0.812 ms with a tight spin against 0.625 ms for two separate regions, so the barrier costs more than the wake-up it removes on this 8P+16E part. Instrumented split per tick: Maya API pre ~100 us (cage inputValue/getRawPoints), W ~200-440 us, H (1 missed row) ~15 us, O ~190-390 us, updateSurface ~90 us. The numerically-identical floor is the two 19.6 MB streams; halving Ainv via packed symmetric storage would change the inverse by its own rounding asymmetry and was not risked. CAVEAT on every number above: the unchanged entry source measured 0.6246, 0.858 and 0.682 ms in three runs during this round, so this machine's bench noise (~+/-0.2 ms) is as large as the effects being compared; the merged-region result should be re-measured with interleaved A/B runs before being written off. The macOS build.sh/bench.sh cannot run on this Windows host; measurements used an equivalent MSVC (/O2 /fp:precise) build plus the same benchmark_node.py invocation and env",
      "slug": "merge_pool_regions",
      "speedup": null,
      "theme": "fold the two per-tick worker wake-ups (W = Ainv@T, then out = H@W) into one pool job with an in-region completion barrier; measured slower on both barrier flavours, so the file is left at the round's entry state"
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored+pointwise",
  "reason": "accepted (6616.12x)",
  "rounds": 2,
  "speedup": 6616.121521709907,
  "stop_reason": "round 2 no-change -- nothing new to compound from",
  "type_name": "rbfWrapDeformer"
}
