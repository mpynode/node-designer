{
  "accepted": true,
  "baseline_ms": 7.7901,
  "bench": {
    "below_floor": true,
    "fingerprint": "checked (1 plug(s))",
    "floor_ms": 15.0,
    "perturbed": [
      "alpha (double)",
      "iterations (int)",
      "ringNbrs[0] (int)",
      "ringWidth (int)",
      "surfaceBlend (double)",
      "input[0].inputGeometry <- pSphereShape1Orig.vtx[0]"
    ],
    "reason": "baseline 7.790 ms is below the 15 ms noise floor even at the largest bench scene (geo=400 array=20000); measured anyway -- every accept must clear 1.15x on two independent timings",
    "rung": [
      400,
      20000
    ],
    "small_rung": [
      40,
      512
    ]
  },
  "best_ms": 2.4305,
  "created": 1788988754.2304215,
  "ledger": [
    {
      "compiled": true,
      "duration_s": null,
      "fix_rounds": 0,
      "hypothesis": "",
      "index": 0,
      "ms": 7.7901,
      "note": "",
      "outcome": "baseline",
      "parity": null,
      "predicted_speedup": null,
      "risk": "",
      "slug": null,
      "speedup": null,
      "theme": ""
    },
    {
      "compiled": true,
      "duration_s": 794.186,
      "fix_rounds": 0,
      "hypothesis": "at 159,602 verts and 20,000 ring elements the gate `len(flat) == N*Kw` can never pass, so every timed tick paid for a 20k MDataHandle walk, a 160k-vertex rest-mesh input pull (measured ~0.9-1.6 ms by itself), and three (N,3) double temporaries whose result is just P; the source reads restMesh and the ring values only inside the gate, so deferring them is a pure reordering with identical output. Measured on this Windows box: 8.06 -> 3.55 ms (ring probe + no double temporaries) -> 2.52 ms (rest-mesh pull deferred). A 4-wide unroll of the remaining float write-back loop measured slower (2.97 ms) and was reverted; the ~2.3 ms left is Maya's own deformer input->output mesh copy.",
      "index": 1,
      "ms": 2.4305,
      "note": "confirmed twice (incumbent under the noise floor): 2.430 / 2.400 ms",
      "outcome": "accept",
      "parity": "pass",
      "predicted_speedup": 2.0,
      "risk": "the O(1) length probe assumes MArrayDataHandle stores elements in ascending logical order (max logical index == last physical element); the gate-fail write-back keeps the exact `p + env*(p - p)` expression in double so -0.0/NaN behaviour matches the original elementwise loop bit for bit",
      "slug": "lazy_gate_inputs",
      "speedup": 3.2051429746965647,
      "theme": "pull the rest mesh and read the 20k ring values only inside the size gate that consumes them, and probe the ring array's length in O(1) instead of walking 20k element handles"
    },
    {
      "compiled": true,
      "duration_s": 833.922,
      "fix_rounds": 0,
      "hypothesis": "the deform body is dominated by the P + env*(P-P) loop widening 480k floats to double and narrowing back; (p-p) is exactly +-0/NaN, env*0 is exactly +-0/NaN, and p + (+-0) is exactly p in either precision, so float loses no bits and runs 4 lanes per op with no cvt; the ring array handle is only observable once Kw > 0, so reading ringWidth first and short-circuiting is the Python's own `and` semantics",
      "index": 2,
      "ms": 2.4158,
      "note": "",
      "outcome": "not-faster",
      "parity": "pass",
      "predicted_speedup": 1.12,
      "risk": "none numerically (identity proven per IEEE case, -0 -> +0 in both forms); the remaining ~2.2 ms is Maya's own input->output mesh copy plus the upstream tweak evaluation, which an empty deform() diagnostic measured at the same minimum as this candidate, so no further in-deform change can move the number; a compute() override to reuse the previous output mesh was considered and rejected because object-group membership and non-count topology changes cannot be verified cheaply",
      "slug": "float_passthrough",
      "speedup": null,
      "theme": "the bench never passes the ring-size gate (20000 ring ids vs 159602*Kw), so the timed path is the envelope pass-through; evaluate it in float, where this exact expression is bit-identical to the double form, and skip building the 20k-element ring handle when ringWidth <= 0"
    }
  ],
  "max_rounds": 6,
  "parity_gate": "authored-only",
  "reason": "accepted (3.21x)",
  "rounds": 2,
  "speedup": 3.2051429746965647,
  "stop_reason": "round 2 not-faster -- nothing new to compound from",
  "type_name": "patchRelax"
}
